## Reasoning Diff｜完整研发与投稿方案（ICLR 科学研究版）

---

### 0. 一页纸方案概览

- **论文标题候选**
  - **主推：** *Reasoning Models Decide What to Read Before They Write It*
  - **备选 1：** *Prospective Premise Dependencies in Long Chain-of-Thought*
  - **备选 2：** *Read-Sets: Latent Dependency Selection in Reasoning Models*
  - 注：主标题不含 "Repair"。局部重算是功能检验手段，不是论文卖点；把它放进标题会把审稿人的心理归类推向系统论文。

- **核心论点（前瞻性 · 可控 · 不可言说）：**
  **在一个推理步被生成之前，模型的隐状态已经确定了这一步将读取哪些输入前提。** 这个读取集合（Read-Set）表征是稀疏可解码的，并且是**前瞻可控**的——在目标步骤尚未生成任何 Token 时交换该表征分量，随后自回归生成的步骤就改为跟随 donor 指定的前提来源；**而模型无法用语言说出这个集合**，自述与监督文本预测器的识别质量显著低于隐状态探针。我们进一步发现该表征会纳入任务结构之外的**虚假前提**，其密度预测推理失败，且选择性移除这些分量可部分恢复正确率。局部重算作为功能检验，测量该表征能否定位输入编辑后必须改变的步骤，并给出失效边界。

  > **为什么这是出人意料的。** "模型能算对，所以依赖信息必然在隐状态里"是一个无法反驳但也无信息量的陈述。本文的主张不是"信息在那里"，而是三件强得多的事：(i) 依赖以**显式稀疏集合**的形式被线性读出，而非仅隐式参与计算；(ii) 它在该步骤**生成之前**就已确定，因而是一个**选择**而非计算的副产品；(iii) 它**可以被前瞻性地改写**，从而改变尚未写出的推理内容。Introduction 必须在第一屏把这三点与"信息当然在那里"区分开。

#### 四项核心贡献

| **编号** | **类型** | **一句话** |
| :--- | :--- | :--- |
| **C1** | 前瞻性识别 | 在步骤生成之前，从隐状态读出该步骤将读取的前提集合；建立任务结构标签、模型编辑响应与隐状态预测三者的对应关系，并检验跨问题、跨改名、跨模型的迁移 |
| **C2** | 前瞻性因果控制 | 在目标步骤生成前的边界交换依赖分量，使后续生成跟随 donor 指定的来源；配合选择性消融、救援，以及随机子空间与非依赖层两类对照，排除子空间干预幻觉 |
| **C3** | 虚假依赖 | 定义 $S=R_{\mathrm{behavior}}\setminus R_{\mathrm{task}}$；检验其密度是否预测推理失败、是否被 no-op 子句抬高、以及选择性消融能否恢复正确率 |
| **C4** | 功能检验与边界 | 无门控局部重算、同预算掩码比较与失效相图；作为表征功能性的端到端验证，而非效率主张 |

- **探索性分析：** 依赖锥传播统计与跨模型表征几何，置于附录；不作为主线成立的前置条件，也不以"定律"措辞呈现。
- **投稿定位：** 面向 ICLR / ACL 的机制与推理研究。保留 14 周研发周期，具体投稿轮次按项目实际进度确定。

---

### 1. 论文叙事弧线（Introduction 推导链）

1. **复用诉求与相似度检索的失效边界**
   Long-CoT 使 Decode 阶段占据推理总开销的 90% 以上。现有复用方案（如 Retrieval-of-Thought）均依赖嵌入相似度驱动。但因果依赖对局部输入高度敏感：两个问题仅相差一个数值时，文本余弦相似度可达 0.99，推导链与最终答案却已反转。依赖必须建立在因果语义之上。

2. **编辑后的计算成本**
   Prompt Cache、CacheBlend、EPIC 等工作推动了输入处理与缓存复用。长推理进一步提出了生成后轨迹的复用问题：输入发生变化后，哪些已有推理步骤仍然有效？*Models Take Notes at Prefill* 附录中的一条 Qwen3-8B 实例显示，旧缓存上原地修改字段后，模型恢复了正确决策，但 Thinking Token 从 clean-prefill oracle 的 249 增长至 926（约 3.7 倍）。这一实例提示：缓存编辑后，计算可能转移到后续推理中。本文直接测量局部修复的解码量、额外 Prefill 和实际耗时。[参考 4]

3. **已有因果归因与本文问题**
   Thought Anchors 同时研究黑盒重采样、白盒注意力聚合和注意力抑制；其中重采样方法采用 100 次 rollout。Thought Branches 通过重复重采样与 resilience 处理被移除内容在下游重新出现的问题。AttriCoT 支持对 Prompt、CoT 和最终答案片段进行局部归因。[参考 1–3]
   本文沿着步骤级分析进一步研究：自然生成时的隐状态如何表征前提依赖，这些表征如何参与下游信息选择，以及它们能否定位输入编辑后需要修复的步骤。比较时区分输入前提归因、步骤间影响和最终答案影响，并采用一致的评价对象。

4. **线索：Token 重合崩溃，语义步骤却活着**
   在 iGSM 上对单个叶子节点做数值微扰，两条轨迹的 Token 序列最长公共子序列重合率（TO）跌至 25% 左右。但如果按语义值逐步对齐，依赖锥之外的步骤产出值几乎没变。这一现象引出本文的问题：能否在隐状态中定位决定哪些步骤响应编辑的依赖信息？Fig. 1 并列展示全链与干净区域的文本变化、语义保持和对齐覆盖率，为后续机制实验建立动机。

5. **从回溯性解码到前瞻性控制**
   探针能读出依赖信息，只说明信息可解码；论文的主张要更强一级——**该信息在步骤写出之前就已确定，并且改写它会改变尚未写出的内容**。因此主干预设计为前瞻式：在目标步骤的第一个 Token 尚未生成时，于对齐的步前边界交换候选依赖分量，随后恢复自回归生成，检查新生成的步骤是否跟随 donor 指定的前提来源。构造当前输出值相同、所需前提来源不同的任务对，使"来源"与"数值"解耦。选择性消融与救援检验该作用能否被移除并恢复；随机正交子空间与非依赖编码层两类对照用于排除"任何方向都能改变输出"的平凡解释。

6. **任务结构之外的读取：虚假依赖**
   任务结构标签 $R_{\mathrm{task}}$ 与模型实际编辑响应 $R_{\mathrm{behavior}}$ 分开标注后，两者的差集本身成为一个可研究对象：某一步的语义值响应了并非其任务祖先的前提。已有工作在行为层观察到，插入一条看似相关但不参与推理链的子句会造成显著的准确率下降，并将其归因于模式匹配。本文给出一个机制层的对应描述并加以检验：无关前提是否进入了步骤的读取集合、进入了哪些步骤、密度是否预测该轨迹出错，以及选择性移除这些分量后正确率是否回升。

7. **局部重算：检验依赖表征的功能**
   输入变化后，利用预测的依赖集合标记受影响步骤，在原轨迹槽位中重算这些步骤并保留干净文本。首先以 Oracle 掩码测试嫁接执行，再以相同重算预算比较隐状态、文本及其他掩码来源。逐步正确性、最终答案正确率和实际处理成本共同衡量局部修复的效果；机制证据由前述干预实验提供。本节的定位是**功能检验**，不作为效率主张。

---

### 2. 理论建模、形式化与完备性证明

#### 2.1 任务设定与语义值映射

- **问题实例：** 输入由前提集合 $P = \{p_1, p_2, \dots, p_m\}$ 构成。
- **推理链轨迹：** 模型生成序列离散化为步骤序列 $T = (s_1, s_2, \dots, s_n)$。
- **步骤语义值 $v(s_i)$：** 每一步产出的离散因果状态（结构化任务中为推导出的参数数值；多跳任务中为实体状态或断言判定）。

#### 2.2 Read-Set：任务依赖、编辑响应与内部表征

**任务结构依赖。** 对任务计算图中的语义节点 $q_i$，定义其完整输入祖先集合：

$$R_{\mathrm{task}}(q_i) = \{p_j\in P:\ p_j\leadsto q_i\text{ in the task DAG}\}.$$

步骤 $s_i$ 通过变量、表达式、出现版本和作用域映射到 $q_i$。结构化任务由解析 DAG 提供该标签，表达正确计算中输入前提到目标节点的传递关系。

**模型编辑响应。** 在模型 $M$、解码配置及固定随机流 $u$ 下，令 $v_i(P;M,u)$ 表示与 $s_i$ 具有相同事件身份的步骤值。对允许的单前提语义扰动集合 $\mathcal D_j$，定义：

$$R_{\mathrm{behavior}}(s_i;P,M,u) = \Bigl\{p_j\in P:\ \exists\delta\in\mathcal D_j,\ v_i(P\oplus\delta(p_j);M,u)\ne v_i(P;M,u)\Bigr\}.$$

该集合以两条轨迹中均有对应事件的比较为基础。事件消失、合并或更换求解路线，单独记录为结构变化。多随机流重复实验报告变化频率及不确定性，不把一次未变化记为已经排除全部允许扰动。

**内部依赖表征。** 记 $Z_i$ 为步骤边界隐状态中的候选依赖表征。分别训练任务依赖头与行为响应头，比较 $Z_i$ 对 $R_{\mathrm{task}}$ 和 $R_{\mathrm{behavior}}$ 的预测，并通过来源交换、选择性消融与救援建立其功能联系。

**统一记号。** 后文 $R(s_i)$ 与 $R^{\mathrm{val}}(s_i)$ 沿用模型值响应集合 $R_{\mathrm{behavior}}(s_i)$；任务结构标签始终显式写作 $R_{\mathrm{task}}$。二者均以原始输入前提为列索引，预测完整“前提—步骤”关系。任务标签的一致性、模型响应的一致性与内部表征的对应程度分别测量。

#### 2.3 依赖解耦：值响应与表面提及

- **值依赖 $R^{\mathrm{val}}(s_i)$：** 对应事件的语义值响应某前提的变化，是行为头的预测对象。
- **表面提及 $R^{\mathrm{surf}}(s_i)$：** 记录步骤文本中的前提名称与符号引用。与值响应分别标注，用于构建文本保留与符号替换操作。
- **结构变化事件：** 步骤消失、合并、重复版本改变或策略分岔，单独报告；不将其直接并入值变化标签。

#### 2.4 因果中介与表征交换框架

设 $H_{i,\ell}$ 为第 $i$ 步结束 Token 在第 $\ell$ 层的隐状态，$Z_i$ 为其中的候选依赖子空间分量。第 $i$ 步的输出已经生成；干预对象是后续步骤 $s_k$（$k>i$）。用 $C_i$ 表示原始前提、已生成文本及其余缓存状态，计算路径写作：

$$C_i\longrightarrow H_{i,\ell}\longrightarrow Y_{>i},\qquad C_i\longrightarrow Y_{>i}.$$

其中 $Y_{>i}$ 包括后续步骤语义值及最终答案。第二条路径显式保留其他位置对未来输出的影响。以 base 与 donor 任务对实施候选分量交换：

$$H'_{i,\ell}=H^{\mathrm{base}}_{i,\ell}+\Pi_Z\bigl(H^{\mathrm{donor}}_{i,\ell}-H^{\mathrm{base}}_{i,\ell}\bigr).$$

对下游评价函数 $g$，定义交换干预效应：

$$\mathrm{IE}_Z=\mathbb E\!\left[g(Y_{>i})\mid \mathrm{do}(H_{i,\ell}\leftarrow H'_{i,\ell}),C_i^{\mathrm{base}}\right]-\mathbb E\!\left[g(Y_{>i})\mid C_i^{\mathrm{base}}\right].$$

$g$ 使用预先指定的来源跟随正确率、目标分支值或断言判定；非数值状态用指示函数评分。干预的核心判据是高层来源交换所预测的下游响应是否被复现，并与非目标分支的保持共同评价。

**执行协议。** 固定 base 的前提和生成前缀，在选定层、选定位置的前向传播期间修改 residual 分量，继续计算该位置后续层及相应 KV，再恢复自回归生成。此前位置的 KV 保持 base 状态；明确记录干预在层内的具体 hook 位置及由此更新的缓存范围。所有处理条件使用同一层、位置、采样配置和随机流集合。donor 来自事件身份可配对的样本，其提取位置与可见前缀一并记录。

**定位协议（前瞻式为主干预）。** 本文的主干预是**前瞻性**的：交换发生在目标步骤 $s_k$ 的**第一个 Token 尚未生成**时，于对齐的步前边界执行；随后恢复自回归生成，评价新生成的 $s_k$ 跟随哪个前提来源。这与"在已生成步骤的步尾读出依赖"是两个量级的证据——前者说明依赖是一个在写出之前已作出的**选择**，并且这个选择可被外部改写。

步尾探针仅用于描述已完成步骤的依赖信息，作为辅助读数。同时比较**步前 / 数值输出前 / 步尾**三个位置的解码性能，以区分"信息选择形成的时间"与"结果编码的时间"；若步前解码性能已接近步尾，即构成"先决定读什么、再写"的直接证据。

**对照协议（排除子空间干预幻觉，两类对照均为必做）。** 仅展示"交换某个方向后输出改变"不足以支持"我们找到了依赖子空间"——任何足够大的扰动都可能改变输出，且干预可能经由非预期通路生效 [参考 7]。因此每一次交换实验都必须配对以下两组对照，且使用完全相同的干预幅度、位置与采样配置：

- **C-rand（随机正交子空间对照）：** 同层、同 Token 位置、同维度 $r$ 的随机正交子空间上施加同等范数的交换扰动。预期：来源跟随率不高于基线水平。
- **C-layer（非依赖编码层对照）：** 在跨层探测曲线上依赖信息显著更弱的浅层（由开发集确定）施加同一交换。预期：来源跟随率显著低于主干预层。

两组对照同时报告**目标分支响应、非目标分支响应、任务正确率与无效输出率**四项，避免以单一"输出是否改变"作为判据。消融（Knock-out）实验沿用同样的两类对照。

#### 2.5 变更传播与 Soundness 证明

给定针对输入前提的编辑子集 $\Delta P \subset P$。定义由 $\Delta P$ 触发的受损依赖锥（Dirty Cone）为：

$$\mathrm{cone}(\Delta P) \triangleq \Bigl\{\, s_i : \hat{R}^{\text{val}}(s_i) \cap \Delta P \neq \emptyset \,\Bigr\}$$

其中 $\hat{R}^{\text{val}}(s_i)$ 为行为头预测的完整输入前提依赖集合，已覆盖输入经中间步骤影响当前步骤的关系，因此直接按集合相交生成掩码。任务结构 Oracle 使用 $R_{\mathrm{task}}$ 按同一规则生成掩码，并单独标识来源。

- **命题 1（确定性 Soundness）：**
  若对所有步骤 $i$ 均满足过近似条件 $\hat{R}^{\text{val}}(s_i) \supseteq R^{\text{val}}(s_i)$，则在受损锥 $\mathrm{cone}(\Delta P)$ 之外的所有干净步骤，其产出的语义值在前提变动下保持不变：
  $$v(s)\big\vert_{P \oplus \Delta P} = v(s)\big\vert_{P}, \quad \forall\, s \notin \mathrm{cone}(\Delta P)$$
  干净步做表面符号替换后保留、仅重新生成受损步骤，所得轨迹在 $P'$ 下严格构成合法推导。

- **命题 2（整链依赖覆盖）：**
  对完整问题或轨迹 $X$，定义分数
  $$a(X)=\max_{i,\ j\in R(s_i)}(1-\hat p_{ij}),$$
  空真依赖集合的分数取 0。以 $N$ 条校准轨迹计算第 $\lceil(N+1)(1-\alpha)\rceil$ 顺序统计量 $q_\alpha$；若索引超过 $N$，阈值取 $+\infty$。令
  $$\hat R(s_i)=\{p_j:1-\hat p_{ij}\le q_\alpha\}.$$
  在模型和打分规则固定、校准与测试轨迹可交换且真标签有效的条件下：
  $$\Pr[\exists i:\ R(s_i)\nsubseteq\hat R(s_i)]\le\alpha.$$
  该事件度量整链依赖漏检；局部重算与文本嫁接的执行错误通过端到端实验另行测量。任务头和行为头使用各自标签分别校准。连续编辑以完整编辑序列为校准单位，在约定轮数及编辑采样协议下取序列内最大分数。[参考 6]

- **拓扑序不变性：**
  复用原轨迹 $T_0$ 的步序，只在对应槽位做替换或保留，避免重新拓扑排序破坏自回归前缀的语言分布。

#### 2.6 虚假依赖（Spurious Dependency）

§2.2 把任务结构依赖与模型编辑响应分开标注。二者的**差集**本身是一个可研究对象，而不仅是记账细节：

$$S(s_i)\;=\;R_{\mathrm{behavior}}(s_i)\setminus R_{\mathrm{task}}(s_i)$$

即：步骤 $s_i$ 的语义值实际响应了前提 $p_j$，但 $p_j$ 并非 $s_i$ 在任务 DAG 上的祖先。模型把与该步计算无关的信息纳入了读取集合。

对称地定义**漏读依赖** $M(s_i)=R_{\mathrm{task}}(s_i)\setminus R_{\mathrm{behavior}}(s_i)$，并单独报告——它对应"该读却没读"，与虚假依赖是不同的失败模式，不应合并成一个"不一致率"。

**度量。** 以非祖先前提为分母的虚假依赖密度：

$$\rho_S(s_i)=\frac{|S(s_i)|}{|P\setminus R_{\mathrm{task}}(s_i)|},\qquad \rho_S(T)=\frac{1}{n}\sum_{i=1}^{n}\rho_S(s_i)$$

分母为空时记 N/A。所有统计相对 §3 的噪声参照报告——**同题不同随机流本身就会产生非零的表观 $\rho_S$，这是本节最主要的混淆来源，必须先扣除**。

**三条可证伪预测。** 三条都可以被数据否定，且否定本身可报告：

| 编号 | 预测 | 判据 |
| :--- | :--- | :--- |
| **P1** | $\rho_S(T)$ 预测该轨迹是否出错 | 控制链长与难度（op）后，$\rho_S$ 对逐题正确性的 logistic AUC 显著高于 0.5；与"链长"这一平凡预测因子做偏相关 |
| **P2** | 注入 no-op 子句会抬高 $\rho_S$，且抬高量与准确率下降量相关 | 同一基础问题的 no-op 版本 vs 原版本的配对 $\Delta\rho_S$；并定位被污染的具体步骤位置 |
| **P3** | 选择性消融虚假分量可部分恢复正确率 | 用 §2.4 的 Knock-out 移除 $S$ 对应分量后准确率变化 $\Delta\mathrm{acc}$，对照 C-rand 与 C-layer，并检查非目标依赖响应与无效输出率是否保持 |

**与已有工作的关系。** 行为层已有观察指出，插入看似相关但不参与推理链的子句会造成显著准确率下降，并被归因于模式匹配而非推理。P1–P3 提供的是一个**机制层的对应描述与检验**：无关前提是否进入了读取集合、进入哪些步骤、以及移除后是否可恢复。**若 P1–P3 全部成立，本节构成"发现现象—定位机制—干预修复"的闭环，其说服力可能高于 §9 的局部重算。** 若 P2 成立而 P3 不成立，同样是有价值的负结果：说明虚假依赖是后果而非原因。

**成本说明。** 本节**不增加新的数据采集**：$R_{\mathrm{task}}$ 与 $R_{\mathrm{behavior}}$ 已在 §4.1 流水线中分别保存，$S$ 是二者的差；P2 需要额外的 no-op 注入子集（见 §7 T2），P3 复用 §2.4 的干预实现。

---

### 3. 指标定义

**Token Overlap（TO）** 衡量轨迹文本重合：

$$\mathrm{TO}(T_0,T_{\mathrm{pert}})=\frac{2\cdot\mathrm{LCS}(T_0,T_{\mathrm{pert}})}{|T_0|+|T_{\mathrm{pert}}|}.$$

分别报告全链 $\mathrm{TO}_{\mathrm{all}}$，以及相同已对齐干净事件上的 $\mathrm{TO}_{\mathrm{clean}}$。干净事件集合由独立任务结构标签确定，并在模型正确、错误轨迹上分组报告。

**Causal Step Preservation（CSP）** 衡量已对齐干净事件的语义保持率。令 $A_{\mathrm{clean}}$ 为两条轨迹均出现并成功对齐的干净事件集合：

$$\mathrm{CSP}_{\mathrm{matched}}=\frac{1}{|A_{\mathrm{clean}}|}\sum_{i\in A_{\mathrm{clean}}}\mathbf1[v_i(P')=v_i(P)].$$

同时报告干净事件对齐覆盖率 $|A_{\mathrm{clean}}|/|T_{\mathrm{clean}}|$、结构变化率、脏事件的值变化率，以及各标签上的探针 precision/recall/F1。分母为空时记为 N/A，并报告样本数。

**噪声参照。** 同题不同随机流重复生成，使用相同事件集合规则测量 $\mathrm{CSP}_{\mathrm{noise}}$ 与 $\mathrm{TO}_{\mathrm{noise}}$。逐题汇总扰动与零扰动差值，并按问题聚合给出区间。保留 $\mathrm{CSP}_{\mathrm{pert}}/\mathrm{CSP}_{\mathrm{noise}}$ 作为描述性比值；Fig. 1 以同区域比较及覆盖率展示文本变化与语义保持的关系。

**干预指标。** 报告来源跟随正确率、目标与非目标分支响应、消融后的任务正确率、救援恢复程度，以及无效输出比例。来源交换的评分目标在实施干预前由高层任务关系确定。**每一项干预指标都必须同时给出主干预、C-rand 与 C-layer 三条数值**（§2.4 对照协议）；主干预相对两条对照的差值是唯一被承认的效应量，绝对值不单独作为结论。前瞻式与回溯式干预分别列出，前瞻式为主表。

**虚假依赖指标。** 报告虚假依赖密度 $\rho_S$ 与漏读密度 $\rho_M$（分开，不合并），并一律给出扣除噪声参照后的值与原始值两列。P1 报告控制链长与 op 后的偏相关及 logistic AUC，附 bootstrap 区间；P2 报告 no-op 注入的配对 $\Delta\rho_S$、$\Delta\mathrm{acc}$ 及被污染步骤的位置分布；P3 报告 $\Delta\mathrm{acc}$ 及 C-rand / C-layer 对照，同时报告非目标依赖响应与无效输出率，避免"通过损伤模型来提高分数"的平凡解释。

**Repairability（解码节省比例）。**

$$\mathrm{Repairability}(T,\Delta P)=1-\frac{\text{修复生成的解码 Token 数}}{\text{全量重推生成的解码 Token 数}}.$$

**重算比例 RR。** $\mathrm{RR}=|\hat D|/n$，其中 $\hat D$ 为重算步骤集合；另报告重算槽位对应的原始 Token 占比。不同掩码按相同原始 Token 预算比较，实际生成长度另行计数。

**正确性与成本。** 分别报告逐步合法率、对任务真值的答案正确率、与完整重推的答案一致率，以及每轮与累计修复成功率。成本分列解码 Token、额外 Prefill Token、探针与调度耗时、端到端延迟。首次建立依赖索引的成本和后续编辑成本分别记录，基线使用相同硬件、精度、批量、上下文长度与缓存策略。

---

### 4. 实验方法体系

```
技术流水线
├── 1. 依赖标注与事件对齐
│   ├── 解析任务 DAG 与完整前提祖先集合
│   ├── 多扰动、多随机流的模型值响应
│   └── 事件身份匹配与结构变化记录
├── 2. 隐状态依赖探针
│   ├── 2 层 MLP 步尾边界检测器
│   ├── 双线性低秩探针：任务头与行为头
│   └── 完整问题 / 轨迹 / 编辑序列级保形校准
├── 3. 因果激活干预
│   ├── 来源—数值解耦与前瞻式表征交换（主干预）
│   ├── 选择性消融、匹配对照与救援
│   └── C-rand 随机正交子空间 / C-layer 非依赖层 两类对照
├── 4. 虚假依赖分析
│   ├── S = R_behavior \ R_task 与 M = R_task \ R_behavior 差集统计
│   ├── no-op 子句注入配对实验
│   └── 虚假分量选择性消融与正确率恢复
└── 5. 无门控局部修复
    ├── Oracle 掩码验证与同预算掩码比较
    ├── 干净文本 Prefill 与受损槽位自回归重算
    └── 离线正确性验证与完整成本记录
```

#### 4.1 任务真值、行为标注与对齐协议

```python
# 伪代码：两个标签来源独立保存
for P in dataset:
    task_dag = parse_task_dag(P)
    for seed in seeds:
        T0 = generate(P, seed)
        events = parse_events(T0)  # 变量 / 表达式 / 版本 / 作用域
        task_labels = map_input_ancestors(events, task_dag)
        records = []
        for premise in P:
            for edit in allowed_edits(P, premise):
                T1 = generate(apply_edit(P, edit), seed)
                pairs, structural_events = align_by_identity(events, parse_events(T1))
                records += record_value_changes(pairs, premise, edit)
                save_structure_events(structural_events, premise, edit)
        save(task_labels, records, seeds=seed)
```

- **结构化任务：** 用事件身份对齐，以变量、表达式、出现版本和作用域区分重复计算。比较解析数值，不用数值相似度决定两个事件是否对应。抽样人工复核事件映射、原步骤正确性和扰动合法性。
- **行为标签：** 保存每种扰动的实际响应与采样频率。有限扫描得到经验行为标签；“未观察到变化”和“已覆盖全部允许扰动且没有变化”分别记录。
- **自然语言迁移：** 先抽取实体、表达式和作用域，采用允许跳过与合并的单调序列对齐，并人工复核小集。结构身份不明确的事件进入未对齐统计，不默认记为值依赖。
- **数据划分：** 同一基础问题、扰动版本与编辑序列保持在同一划分内。探针训练、干预方向拟合、校准与最终测试划分分离；几何对齐使用独立配对集合。

#### 4.2 步边界检测器与双线性探针

- **步边界检测器：**
  2 层 MLP 分类头（$\mathrm{Hidden}=256, \mathrm{ReLU}$），输入各 Token 隐状态，二分类判定是否为步尾边界。由 iGSM 的确定性句法标记提供监督。在通用长思维链任务上零样本迁移 $\mathrm{F1} > 0.95$，替代 `\n\n` 等脆弱启发式。

- **双线性探针与保形校准：**
  $$\hat{p}_{ij} \equiv \mathrm{score}_{ij} = \sigma(h_i^\top U V^\top e_j + b), \qquad U, V \in \mathbb{R}^{d \times r}, \; r=64$$
  - $h_i$：第 $i$ 步边界 Token 的隐状态，截取自模型 60%–75% 相对深度的层；
  - $e_j$：第 $j$ 个前提 Span 在相同层上的平均池化向量；
  - **损失函数：** 加权交叉熵，$\lambda_{\text{FN}} = 10$，偏向高召回。
  - **监督标签：** 两个头沿用同一双线性形式，分别学习 $R_{\mathrm{task}}$ 与经验 $R_{\mathrm{behavior}}$；标签、阈值及结果单独保存。
  - **保形校准：** 按 §2.5 的完整轨迹最大分数校准；连续编辑按完整序列校准。同时报告整链漏检率、预测集合大小与实际重算比例。
  - **公平对照：** 文本侧依赖预测器使用相同输入、截至当前边界的步骤文本、同样标签与数据划分；微调 Verbalizer 使用相同监督样本。比较步前、数值输出前与步尾探针，并在变量改名与未见依赖组合上测试。

---

### 5. 结果章节规划（按主张组织）

> **写作硬约束：结果章的每个小标题必须是一句可被证伪的陈述句，不得使用 "Comparing…"、"Analysis of…"、"Experiments on…" 这类过程性标题。** 过程性标题会把论文的心理归类推向工程报告。

```
├── 5. Premise Dependencies are Decodable Before the Step is Written   (Fig. 1, 2)
├── 6. Dependency Representations Prospectively Control
│      Which Premise a Step Reads                                      (Fig. 3)
├── 7. Spurious Premise Dependencies Predict Reasoning Failures        (Fig. 4)
├── 8. Models Cannot Verbalize the Dependencies They Compute           (Fig. 5)
└── 9. Local Repair and Failure Modes                                  (Table 1, Fig. 6)

附录：连续编辑（Fig. S1）、依赖锥传播统计（Fig. S2）、
      跨模型表征几何（Fig. S3）、检索相似度对照（Fig. S4）
```

> **本节所有数值均为待测的预期区间，不是已测结果。** 早期草稿中出现过若干形如 "F1 > 0.9"、"下降 85%"、"R² > 0.94" 的数字，此处一律改写为 `[预期 … / 待测]`，以免锚定实验预期或被误抄进论文草稿。

#### 第 5 节：Premise Dependencies are Decodable Before the Step is Written

**动机（Fig. 1）。** 单叶子扰动下，全链 Token 重合率 TO 大幅下跌，而依赖锥之外的已对齐事件语义值基本保持。Fig. 1 并列展示全链与干净区域的文本变化、语义保持、零扰动噪声参照及对齐覆盖率，建立"文本大改、语义局部保持"的现象。`[预期 TO ~0.25、CSP ~0.75 / 待测]`

**解码性（Fig. 2a）。** 跨层探测热力图，给出探针 F1 随相对深度的变化。`[预期峰值出现在 0.6–0.75 相对深度 / 待测]` 同时对照任务依赖头与行为响应头。

**时机（Fig. 2b，本节的主张所在）。** 比较**步前 / 数值输出前 / 步尾**三个位置的解码性能。**若步前性能已接近步尾，即构成"模型先决定读什么、再写"的直接证据**，这是本节标题中 "Before the Step is Written" 的全部依据；若步前显著低于步尾，则本节标题必须回退为 "…are Decodable from Reasoning States"，并相应下调 §0 的核心主张。**这是全文第一个必须诚实处理的分叉点。**

**泛化与迁移（Fig. 2c）。** 跨问题、变量改名、数值变化、未见图组合的测试。**跨模型探针迁移从附录提升至主文**：在模型 A 上训练的探针不经适配直接应用于模型 B 的解码性能，以及无标签配对适配后的性能。该结果与"步骤重要性归因不跨模型迁移"的既有结论形成直接对照——因为本文的依赖标签锚定在任务结构上而非单条轨迹上，跨模型一致性是一个可检验的预测而非假设。几何层面的 Procrustes 对齐分析留在附录 Fig. S3。

模型正确轨迹与错误轨迹分开分析，展示任务依赖与行为响应的一致与偏离（该偏离在第 7 节被正式定义为虚假依赖）。

#### 第 6 节：Dependency Representations Prospectively Control Which Premise a Step Reads

**本节主干预是前瞻式交换，回溯式读数仅作辅助。**

**来源—数值解耦。** 构造两类任务：目标计算分别要求读取 $a$ 或 $b$，先令 $a=b$，使目标输出值相同而来源不同；再构造来源相同、数值不同的任务对。对变量身份、位置和表述作平衡，划分未见变量名及依赖组合的测试集。

**前瞻式表征交换（Fig. 3a）。** 在目标步骤 $s_k$ **第一个 Token 尚未生成**时，于对齐的步前边界按 §2.4 交换候选依赖分量，随后恢复自回归生成。在 $a\ne b$ 的数值配置下重复交换，检验新生成的 $s_k$ 是否跟随 donor 指定的来源；donor 分量随每个数值条件重新提取，base 前缀与无关缓存按协议保留。目标与非目标分支的预期变化在干预前由任务关系预先列出，避免以"输出发生变化"评分。Fig. 3a 为来源响应矩阵。同时报告回溯式（步尾）交换结果作为对比，说明两者的证据强度差异。

**两类必做对照（Fig. 3b）。** C-rand（同层同维随机正交子空间）与 C-layer（非依赖编码浅层）在相同干预幅度下的来源跟随率。**主干预相对这两条对照的差值是唯一被承认的效应量。** 缺少这两条，"我们找到了依赖子空间"会被直接质疑为"你找到了某个能改变输出的方向" [参考 7]。

**Knock-out（选择性消融，Fig. 3c）。** 用 INLP 构造候选子空间并正交移除对应分量，报告目标依赖响应、非目标依赖响应、任务正确率与无效输出率四项。`[预期目标响应显著下降、随机对照接近 0 / 待测]` 对照同样包含 C-rand 与 C-layer。

**Rescue（救援，Fig. 3d）。** 在同一消融位置补回匹配的原始候选分量，测量目标响应恢复；错误来源分量与随机分量作为对照。INLP、交换子空间及超参数均在开发集拟合，最终测试固定。[参考 7]

#### 第 7 节：Spurious Premise Dependencies Predict Reasoning Failures

本节检验 §2.6 的 P1–P3，**不增加新的数据采集**（$S$ 与 $M$ 由已保存的两组标签求差得到）。

**基线扣除先行。** 首先用同题不同随机流报告表观 $\rho_S^{\mathrm{noise}}$ 与 $\rho_M^{\mathrm{noise}}$。本节所有结论均以扣除该参照后的量陈述，并同时给出原始值。**若扣除后 $\rho_S$ 不显著大于 0，本节直接转为负结果报告，不做后续 P2/P3。**

**P1（Fig. 4a）。** 控制链长与 op 之后，$\rho_S(T)$ 对逐题正确性的 logistic AUC 与偏相关，附 bootstrap 区间；并与"链长"这一平凡预测因子对比，说明增量预测力。按扰动深度与任务层（T1/T2/T3）分面。

**P2（Fig. 4b）。** 同一基础问题的 no-op 注入版本与原版本配对，报告 $\Delta\rho_S$ 与 $\Delta\mathrm{acc}$ 的关系，并定位被污染步骤在轨迹中的位置分布（靠前 / 靠后、是否集中在计划性步骤）。这一步把行为层已知的"插入无关子句导致大幅掉点"落到具体步骤与具体前提上。

**P3（Fig. 4c）。** 用 §2.4 的 Knock-out 移除 $S$ 对应分量，报告 $\Delta\mathrm{acc}$，对照 C-rand 与 C-layer，并同时报告非目标依赖响应与无效输出率，排除"通过损伤模型来提高分数"的平凡解释。

**漏读依赖 $M$ 单独报告**，不与 $S$ 合并为"不一致率"——二者是不同的失败模式。

**结果分流。** P1–P3 全部成立 → 本节构成"发现现象—定位机制—干预修复"的闭环，在主文中的位置优先于第 9 节；P2 成立而 P3 不成立 → 作为有价值的负结果报告，说明虚假依赖是后果而非原因；P1 不成立 → 本节降为附录的描述性统计。

#### 第 8 节：Models Cannot Verbalize the Dependencies They Compute

**注意力基线。** 使用前提 Token 注意力均值、Attention Rollout 及开发集选定的头聚合方式预测同一依赖标签，阈值在验证集确定。`[预期与因果标签仅弱相关 / 待测]` 结合前提位置、语法 Token 与 Sink Token 分组分析其误差来源。

**模型自述四档。**（1）Zero-shot；（2）5-shot ICL；（3）推理后反思依赖；（4）监督微调 Verbalizer。第（4）档使用与探针**完全相同**的监督样本、划分与可见前缀，并记录训练预算——这是本节结论成立的关键，否则"说不出来"可以被归因于没训过。`[预期四档均显著低于隐状态探针 / 待测]`

**监督文本预测器。** 输入原始题目与截至当前边界的步骤文本，用相同依赖标签训练预测器，比较依赖识别质量及进入第 9 节修复流程后的效果。

Fig. 5 并列展示所有方法的 precision / recall / F1、整链覆盖与预测集合大小；任务标签与行为标签分面展示。**本节的结论句式是"存在一个模型内部持有、但其自身输出无法表达的量"**，而不是"我们的探针比 baseline 强"。

> **风险挂钩 R4：** 若监督文本预测器达到与隐状态探针相近的表现，本节标题不成立，必须改写；此时主文重心移向第 6 节与第 7 节的功能性证据。

#### 第 9 节：Local Repair and Failure Modes

**本节定位为功能检验，不作为效率主张。主文只保留一张表与一张相图。**

**Oracle 修复（第一阶段即执行）。** 用解析任务依赖生成 Oracle 掩码，在映射可核查且原步骤正确的轨迹上测试局部嫁接；另在小集上用充分扰动扫描与人工复核构建行为参考掩码。两种掩码分开报告。记录槽位开始与停止边界、逐步正确性、最终答案与成本，以区分掩码错误与执行错误。

**单次编辑（Table 1）。** 在 T1/T2/T3 下执行无门控局部重算。**主文对照精简为 5 条**：Full Recomputation、Task Oracle、Linear Truncation、Prompt-Instruction、监督文本掩码。**移入附录**：Behavior Reference、Retrieval-of-Thought、匹配预算的随机掩码。参考掩码只在具有相应标签的子集比较。

**同预算比较。** 以重算槽位对应的原始 Token 数控制预算，在多个预算点比较不同掩码的修复正确率；实际解码长度、额外 Prefill 与延迟另外测量。端到端正确率—成本曲线放附录，主文只给固定预算下的结果。

**失效相图（Fig. 6，保留在主文）。** 横轴为扰动逻辑深度，纵轴为约束复杂度。区分依赖漏检、重算错误、嫁接接口错误、策略分岔与约束退化。离线验证器仅用于评分与分类，不触发运行时回退，也不筛掉失败样本。**这张图是边界条件的科学陈述，不是系统稳健性展示。**

**连续编辑移入附录（Fig. S1）。** 连续 $k\in\{1..5\}$ 次编辑、序列级校准、每轮正确率与累计成本——属于工程稳健性，不服务于第 5–8 节的任何主张。保留完整协议（保留文本在当前前缀下重新 Prefill 后提取状态，不沿用旧前缀隐状态），但不占主文版面。

#### 附录分析 S1：连续编辑稳健性

见上。保留原协议与全部计数。

#### 附录分析 S2：依赖锥传播统计（Fig. S2）

保留两参数候选拟合：

$$\frac{|\mathrm{cone}|}{n}\approx1-\exp\{-\lambda(1-x)^\gamma\},\qquad x=\frac{\mathrm{depth}_{\mathrm{pert}}}{\mathrm{depth}_{\max}}.$$

分别计算不调用模型的任务 DAG 锥、模型行为响应锥与探针预测锥；统一深度方向及分母，比较图结构预测与模型偏离。跨模型、难度及图生成设置留出测试，与简单图统计拟合比较。

> **措辞约束：本节一律不使用"定律""普适"等表述，也不以 $R^2$ 作为贡献点。** 两参数拟合配高 $R^2$ 在审稿中极易被判为 curve fitting；本节的功能是描述性对照，不是主张。

#### 附录分析 S3：跨模型表征几何（Fig. S3）

主文已承载跨模型**探针迁移**（第 5 节）。本附录保留几何层面的分析：对不同维度的表示先在独立配对集合上学习共同维度映射，再作 Procrustes 对齐，报告配对样本数、是否使用标签与测试划分。明确区分不经适配的直接迁移、无标签配对适配与监督适配。模型间任务标签相同不作为行为或几何一致的前提。

#### 附录分析 S4：检索相似度对照（Fig. S4）

对每一对 $(P, P\oplus\Delta P)$ 计算文本嵌入余弦相似度，与"最终答案是否改变"作散点。用于支撑 Introduction 中"相似度在最小编辑处最不可用"的论断。成本极低（仅需嵌入计算），**不得因精简而删除**——它是与相似度检索复用路线对话的唯一必要证据。

### 6. 相关工作与后续方向

#### 6.1 与 V-Probing 的关系

V-Probing 研究变量必要性、变量对递归依赖，以及解答过程中已知状态、数值和下一步可计算性。本文在这一研究基础上，将自然长推理轨迹中的步骤依赖识别、来源交换与编辑后局部修复连接起来。[参考 5]

| 比较维度 | V-Probing | 本工作 |
| :--- | :--- | :--- |
| **探测对象** | `nece(A)`、`dep(A,B)`、`known(A)`、`value(A)`、`can_next(A)`、`nece_next(A)` | 步骤相关的完整前提依赖及实际编辑响应 |
| **探测时机** | 依赖任务在题目描述后；状态与数值等任务在生成过程中的句子边界 | 自然生成轨迹的步前、数值输出前及步尾 |
| **探测方式** | 添加变量查询 Token、输入嵌入的低秩更新及分类头 | 在冻结模型自然生成的状态上训练外部探针 |
| **功能验证** | 分析隐式推理过程及错误相关状态 | 来源—数值解耦、依赖分量交换、消融救援与局部修复 |

#### 6.2 后续方向备忘

- **Repairability 奖励：** 留作后续训练研究，当前实验不包含 RL 训练，也不以该愿景承担论文贡献。
- **生产运行时：** Cone Gate、Verifier Fallback 和 KV 位置重绑定留作后续工程项目。当前实现集中于无门控修复、离线验证及成本测量。

---

### 7. 数据集构建与系统扫描网格

为避免单纯堆砌数据源，各测试层均针对具体的验证假说设计：

- **T1：iGSM-Diff（解析真值基石）。** 基于 FAIR iGSM 生成器（$\mathrm{op} \in \{5, 10, 15, 21\}$）。由于底层参数依赖 DAG 完全由生成规则确定，它为任务依赖头提供解析真值，并为模型行为响应与内部干预提供可核查的任务参照。
- **T2：GSM-Symbolic & GSM-Plus（自然语言数学迁移）。** 覆盖数值微扰、算子逆转与增删限制条件三类变化，用于验证从合成符号向自然语言数学表述迁移时的因果捕获稳定性。
- **T2-noop：无关子句注入配对集（虚假依赖专用）。** 为 §2.6 的 P2 构建：对同一基础问题生成"原版 / 注入一条看似相关但不参与推理链的子句"的配对样本，注入子句的位置（靠前 / 中段 / 靠后）与表面相关度分层控制。注入子句本身即成为一个**已知的非祖先前提**，因此 $\rho_S$ 是否在其上升高是一个有明确真值的检验，而不是事后解读。T1 上可用 iGSM 的图生成规则直接构造等价的注入变体，作为解析真值对照。
- **T3：真实多跳负载（任务泛化）。** 包括多跳问答（HotpotQA / MuSiQue，替换单个关键支撑文档检验长程依赖）与代码推理（HumanEval-Perturb，修改单条 Spec 约束并以单元测试为客观裁判）。
- **T4：极端相变样本集（边界分析）。** 收集解题策略突变（如代数法突变为穷举法）以及约束矛盾导致的无解题，专门用于绘制失效相图。

**实验扫描网格：** 基础维度为 $\mathrm{op} \times \text{扰动节点深度}(\text{叶/中/根}) \times \text{参数量 } \mathrm{ip}$；连续编辑序列为 $k \in \{1..5\}$ 轮级联微调。

---

### 8. 算力配置与科学评测实现

#### 8.1 硬件与模型分配
- **模型库：** Qwen3-8B / 14B，DeepSeek-R1-Distill-Qwen-7B / 14B / 32B。
- **硬件分工：** 8 × RTX 5090 承担 T1/T2 离线扰动扫描与轻量探针训练；4 × A100-80G 承担 32B 模型的因果干预与长链端到端验证。

#### 8.2 当前实现范围

```
科学评测流程（--eval-mode scientific）
├── 自然轨迹与隐状态采集
├── 任务 / 行为标签、探针与轨迹级校准
├── 来源交换、消融、救援
├── 无门控文本嫁接与受损槽位重算
├── 离线逐步验证与最终答案评分
└── 解码量、额外 Prefill、探针耗时与端到端延迟
```

当前运行时禁用 Cone Gate 与 Verifier Fallback。验证器在输出后评分，不改变执行路线；保留所有失败计数。Oracle 与预测掩码共用嫁接执行器，基线共用计时和成本统计协议。

---

### 9. 第一阶段验证与项目决策

**第一周配置。** 保留 iGSM 500 题、四档 op 难度、Qwen3-8B 与 R1-Distill-Qwen-7B，以及 $T_0(seed=0)$、$T'_0(seed=1)$、$T_{\mathrm{pert}}(seed=0)$ 三条基础轨迹。另在人工核查小集上增加多扰动与随机流重复，区分值响应、结构变化和采样波动。

| 优先实验 | 直接产出 | 决策依据 |
| :--- | :--- | :--- |
| **标签与事件核查** | 解析任务依赖、事件身份、行为响应记录及测量误差 | 先修正标注与对齐错误，再扩充训练数据 |
| **Oracle 局部修复** | 小集逐步合法率、答案正确率、成本和执行失败分类 | 判断局部复用空间，并区分掩码与嫁接接口问题 |
| **探针与监督文本基线** | 同标签、同划分、同可见前缀下的预测及修复表现 | 判断隐状态信息的额外价值 |
| **来源解耦与干预冒烟** | 50 条样本上的**前瞻式**来源交换、消融与救援响应矩阵，**同批次含 C-rand 与 C-layer 两组对照** | 判断目标依赖是否被选择性改变、非目标功能是否保持，以及主干预相对两组对照是否有可见差值 |
| **虚假依赖首次计数** | 扣除噪声参照后的 $\rho_S$ 与 $\rho_M$；小规模 no-op 注入的配对 $\Delta\rho_S$ | 判断 §2.6 的 P1–P3 是否值得投入第 8 周；若扣除后 $\rho_S$ 不显著，第 7 节立即降级 |

第一周完成前两项及后两项原型，第 2–3 周完成首轮对照。Fig. 1 的 TO/CSP 保留为动机图，项目决策由标签可靠性、Oracle 修复和依赖信息功能共同决定。

**推进规则。** Oracle 修复失败时先修复步骤接口；若预测信号明确而交换实验不稳定，继续定位干预位置与表征；若功能性成立而修复收益有限，按成本与正确性结果调整复用实验。原方案中单层消融下降 $>50\%$ 的检查保留为诊断读数，并同时检查非目标响应与救援效果。

**结果分流。** 区分测量问题、机制证据和执行收益。CSP 较低时先检查事件对齐、原轨迹错误和策略变化，不预设其对应“因果混沌”论文；CSP 较高时继续通过功能实验建立机制证据。

---

### 10. 项目风控矩阵

| 风险项 | 等级 | 表现形式 | 应对方案 |
| :--- | :---: | :--- | :--- |
| **R1：语义保持与测量混淆** | 中低 | 干净事件值变化或对齐覆盖率降低 | 核查事件身份、步骤正确性及结构变化，按第一阶段实验定位原因 |
| **R2：跨域泛化召回不足** | 中 | T1 探针在 T3 上性能衰减 | T1+T2 联合训练；报告不同任务和编辑类型的召回、集合大小及修复表现 |
| **R3：嫁接执行失效** | 中 | 正确掩码下仍出现步骤或答案错误 | 以 Oracle 小集检查槽位边界、输出接口与前缀处理；引导 Token 如被采用，计入成本并单独消融 |
| **R4：隐状态相对文本增益有限** | 中 | 同监督文本基线达到相近表现 | 检验来源解耦与交换的功能证据，依据预测、干预和修复结果确定主文重点 |
| **R5：解码节省未转化为耗时收益** | 中 | Prefill 与探针开销抵消生成节省 | 分列处理成本，按相同硬件与缓存协议报告正确率—成本曲线 |
| **R6：相关工作差异不准确** | 中 | 比较对象或方法范围混淆 | 按 §6.1 及原论文明确已有贡献，比较同一依赖对象和评价协议 |
| **R7：步前解码不成立** | 中 | 步前探针性能显著低于步尾 | 主张回退为"可从推理状态解码"，主标题与 §0 同步下调；第 6 节的前瞻式交换改为在能解码的最早位置执行，并如实报告该位置 |
| **R8：前瞻式交换不稳定或被对照追平** | 中 | C-rand / C-layer 的来源跟随率接近主干预 | 先定位层与 Token 位置，再检查子空间维度 $r$ 与干预幅度；若差值仍不显著，按 [参考 7] 的框架报告为子空间干预幻觉的负结果，主文重心移向第 5、7 节 |
| **R9：虚假依赖被采样噪声解释** | 中 | 扣除噪声参照后 $\rho_S$ 不显著 | 第 7 节转为负结果报告并降为附录；同时检查事件对齐与结构变化是否被误计入值变化 |

---

### 11. 14 周研发排期

- **第 1 周：** 三轨迹采集、解析标签和事件人工核查；跑通 Oracle 嫁接；产出 Fig. 1 原型及来源交换小集。
- **第 2–3 周：** 多扰动行为标注与结构变化记录；完成 Oracle 修复首轮对照、轻量探针和同监督文本基线、交换消融救援冒烟实验；形成第一阶段决策。
- **第 4–5 周：** 训练双线性任务头与行为头、2 层 MLP 边界检测器；完成轨迹级校准；产出 Fig. 2（含**步前 / 数值输出前 / 步尾**三位置比较，即第 5 节主张的判据）与文本、注意力、自述四档对照 Fig. 5。**第 5 周结束时按 R7 决定是否保留"Before the Step is Written"这一主张。**
- **第 6–7 周：** 完成来源—数值解耦与**前瞻式**候选分量交换、选择性消融与救援，绘制 Fig. 3；**C-rand 与 C-layer 两类对照与主干预同批次运行，不得延后**；测试变量改名和未见依赖组合。
- **第 8 周：** 虚假依赖分析（§2.6 / 第 7 节）。先做噪声参照扣除，再依次检验 P1、P2、P3，绘制 Fig. 4；T2-noop 配对集在本周前完成构建。**本周结束时按第 7 节"结果分流"规则确定该节在主文中的位置。**
- **第 9 周：** 扩展 T2/T3 与不同模型的核心验证；完成跨模型探针迁移（并入 Fig. 2c）；完成单次编辑及同预算掩码比较，形成 Table 1。
- **第 10 周：** T4 边界实验与失效分类，绘制 Fig. 6；完成主文证据核对。
- **第 11 周：** 附录内容集中处理——连续编辑与序列级校准（Fig. S1）、正确率—成本曲线、被移出主文的三条基线。**主文存在未完成验证时，一律优先补齐主文。**
- **第 12 周：** 附录探索分析——锥传播统计（Fig. S2）、跨模型几何映射（Fig. S3）、检索相似度对照（Fig. S4）。
- **第 13 周：** 整理科学评测代码、标签协议与复现实验配置，完成正文初稿。
- **第 14 周：** 图表、术语、引用与主张—证据对应检查，完成投稿稿件。

---

### 12. 启动前自检清单

- [ ] 科学评测禁用门控和 Fallback；离线验证与计时完整记录失败样本。
- [ ] 检索近期 activation patching、reasoning dependency、causal mediation CoT 文献，核对相关工作原文与版本。
- [ ] 导出 iGSM 任务 DAG；任务依赖、经验行为响应和结构变化使用独立字段。
- [ ] 用事件身份进行对齐，人工核查步骤映射与正确性，报告未对齐和结构变化计数。
- [ ] 探针与文本基线使用相同划分和可见前缀；同题扰动版本保持在同一数据划分。
- [ ] 明确干预层内位置、Token 位置和 KV 更新范围；干预发生在被评价的目标步骤生成之前。
- [ ] 对来源—数值解耦任务预先列出高层交换的目标响应，并设置非目标分支与救援对照。
- [ ] **每次交换与消融实验都配 C-rand（同层同维随机正交子空间）与 C-layer（非依赖编码浅层）两组对照，干预幅度、位置、采样配置完全一致；效应量以相对对照的差值报告。**
- [ ] **主干预为前瞻式：交换发生在目标步骤第一个 Token 生成之前；步尾回溯式结果单独列出，不与前瞻式混报。**
- [ ] **虚假依赖 $S$ 与漏读依赖 $M$ 分开保存与报告，不合并为"不一致率"；所有 $\rho$ 统计先扣除同题不同随机流的噪声参照。**
- [ ] **T2-noop 配对集在第 8 周前构建完成，注入位置与表面相关度分层。**
- [ ] **结果章小标题逐条检查是否为可证伪的陈述句；若第 5 节的步前解码不成立，同步回退 §0 核心主张与主标题。**
- [ ] 保形校准按完整轨迹或完整编辑序列实施，记录集合大小和重算比例。
- [ ] 第一阶段即测试 Oracle 嫁接，后续比较使用同一执行器与一致预算口径。
- [ ] 同时统计解码 Token、额外 Prefill Token、探针开销与端到端延迟。

> **方案原则：** 算力数字是验证工具，因果机理是科学归宿。

---

### 参考文献与核查入口

1. [Thought Anchors: Which LLM Reasoning Steps Matter?](https://arxiv.org/html/2506.19143v1) — 三种归因方法与附录 D 的跨模型验证。
2. [Thought Branches: Interpreting LLM Reasoning Requires Resampling](https://proceedings.iclr.cc/paper_files/paper/2026/hash/954d303839bf7fc458acea7384322255-Abstract-Conference.html) — 重复重采样、resilience 与轨迹分布分析。
3. [Local Causal Attribution of Chain-of-Thought Reasoning](https://arxiv.org/html/2606.21821v1) — AttriCoT；附录 C.2 的 Prompt 与输出片段归因。
4. [Models Take Notes at Prefill: KV Cache Can Be Editable and Composable](https://arxiv.org/html/2606.17107v1) — 附录单条实例中的 249 / 926 thinking tokens。
5. [Physics of Language Models: Part 2.1, Grade-School Math and the Hidden Reasoning Process](https://arxiv.org/html/2407.20311v1) — §4.1 与附录 B 的 V-Probing 任务和探测时机。
6. [A Gentle Introduction to Conformal Prediction and Distribution-Free Uncertainty Quantification](https://arxiv.org/html/2107.07511v6) — 校准分数、有限样本分位数与覆盖条件。
7. [Is This the Subspace You Are Looking for? An Interpretability Illusion for Subspace Activation Patching](https://arxiv.org/abs/2311.17030) — 子空间干预的功能控制与机制定位。
