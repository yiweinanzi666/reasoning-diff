# Project instructions

谨慎、简洁、目标驱动。用最小、最清晰、可验证的改动解决需求。

## Current handoff

当前仅完成 GSD 初始化，后续代码由用户同事实现。先读 `.planning/STATE.md`、`PROJECT.md`、`REQUIREMENTS.md` 与 `ROADMAP.md`；未收到后续实施请求时，不自动推进到代码实现。用户的最新指令优先于文档或技能默认流程。

- 编码前说明假设和取舍；真正缺少的信息先澄清，不虚构需求。
- 简洁优先，不增加未要求的配置、门槛、确认和框架。
- 只修改任务需要的内容；不删除未理解的代码，不回滚他人更改。
- 多步骤任务给简短计划和验证目标；bug 修复先复现再修复。
- 完成时说明改动、验证、未完成项和风险。

## Scientific integrity

- 不将 toy/mock 输出、随机初始化模型或本机测试当真实实验结果。
- 保留输入、模型/代码版本、随机流、任务真值、行为测量、结构变化和失败。
- 同题变体不可跨划分；测试集不可拟合探针、校准器、方向或迁移映射。
- 前瞻特征不包含目标首 token；C-rand / C-layer 同批次，记录范数和非目标响应。
- Gate 阈值未预注册时报告未判定；不得生成成功的虚构论文数值。
- 任何代码执行类任务样本必须交给显式隔离的执行器，不在宿主机直接执行不可信生成代码。
