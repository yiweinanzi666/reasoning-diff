# Phase 4 Plan 04-01

**Goal:** Prospective swap, INLP, rescue, matched C-rand/C-layer.

**Must have**
1. H' = H_base + Π_Z(H_donor-H_base).
2. INLP iterates on already-projected H.
3. C-rand/C-layer require the actual main-intervention norm.
4. Intervention report uses relative-to-controls only; no invented accuracies.
5. Hook last token; cache isolated.

**Verify:** `tests/test_science.py::test_swap_formula`, `tests/test_review_regressions.py` C-03/C-04
