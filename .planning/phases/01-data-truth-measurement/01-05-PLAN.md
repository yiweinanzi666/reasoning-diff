---
phase: 01-data-truth-measurement
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - src/reasoning_diff/graphs.py
  - src/reasoning_diff/annotations.py
  - src/reasoning_diff/schema.py
  - src/reasoning_diff/splits.py
  - tests/test_graphs_annotations.py
  - tests/test_splits.py
  - tests/fixtures/annotation_sidecar.jsonl
autonomous: true
requirements:
  - DATA-02
  - DATA-03
  - SURF-01
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-02: 独立标注导入后 graph_status 可为 complete/partial/unknown；unknown 节点不能当 task negative"
    - "SURF-01: 每步 R_surf 与值依赖 / R_task 分字段保存"
    - "D-03: 同 base_group_id 的数值变体、改名、no-op、编辑序列、donor 对、双模型轨迹只有一个角色"
    - "D-03: fit_eligible=false 的组强制 test，且与拟合组集合无交集"
  artifacts:
    - src/reasoning_diff/graphs.py
    - src/reasoning_diff/annotations.py
    - src/reasoning_diff/splits.py
    - tests/test_graphs_annotations.py
    - tests/test_splits.py
  key_links:
    - "annotation sidecar → IndependentGraph → Task.graph_ref"
    - "family_id + base_group_id → assign_split"
  prohibitions:
    - statement: "Do not fill missing graph edges with false"
      status: required
      verification: pytest
    - statement: "Do not split variants of the same base across fit and test"
      status: required
      verification: pytest
---

<objective>
完成独立图/标注导入、表面提及与值依赖分离，以及六角色共组划分（含 test-only 锁与泄漏检查）。

Purpose: 后续探针/干预依赖这些身份与角色合同。
Output: annotations 导入、SURF 字段、splits 泄漏测试。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/reasoning_diff/splits.py
@src/reasoning_diff/graphs.py
@src/reasoning_diff/schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Independent annotation import and graph coverage</name>
  <files>src/reasoning_diff/graphs.py, src/reasoning_diff/annotations.py, tests/test_graphs_annotations.py, tests/fixtures/annotation_sidecar.jsonl</files>
  <read_first>
    - src/reasoning_diff/graphs.py
    - .planning/research/ARCHITECTURE.md (Independently Annotated Natural-Language DAGs)
    - 01-CONTEXT.md D-02
  </read_first>
  <behavior>
    - import_annotation_jsonl builds nodes/edges with annotator, revision, review_status
    - coverage complete only when every required node/edge is known; otherwise partial or unknown
    - task_label for an unmapped premise is unknown, not 0
    - Empty graph (no nodes) is distinct from graph_status unknown
    - CoT or probe scores cannot be passed as annotation source; import rejects source model_cot
  </behavior>
  <action>
    扩展 graphs.py：IndependentGraph(graph_kind, status, nodes, edges, expressions, coverage)。import_annotation_jsonl(path) 在 annotations.py。侧车字段：premise → assertion/operator → target，node 身份、scope、关系证据、annotator、revision、review_status。

    只有祖先集合声明完整时，未列入集合的前提才能成为可靠 task negative。否则 task_known=false。空图 status 用 empty 或 complete+zero nodes 的显式区分：用 graph_status=unknown 表示“没有图信息”，用 complete 且 nodes=[] 仅当标注者明确“无中间事件”。D-02：被测 CoT 禁止作为真值来源。
  </action>
  <verify>
    <automated>python -m pytest tests/test_graphs_annotations.py -q --tb=short -k "import or coverage or unknown"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - import rejects source model_cot
    - A partial graph test shows unknown premises are not false
    - empty graph and unknown graph compare unequal
  </acceptance_criteria>
  <done>独立标注可导入；缺失不为假负。</done>
</task>

<task type="auto" tdd="true">
  <name>SURF-01 surface mentions vs value dependence</name>
  <files>src/reasoning_diff/annotations.py, src/reasoning_diff/schema.py, tests/test_graphs_annotations.py</files>
  <read_first>
    - Reasoning-Diff-修订方案-v3 (1).md §2.3 (若需核对字段名)
    - .planning/REQUIREMENTS.md SURF-01
    - src/reasoning_diff/schema.py
  </read_first>
  <behavior>
    - An event can list surface_mention_premise_ids without those ids appearing in task_ancestors
    - Value-dependence labels live in task_label / R_task; surface mentions live in surface_mentions
    - A step that names p1 but does not depend on p1 has p1 in surface_mentions only
  </behavior>
  <action>
    为 Event 或并行 SurfaceLabel 增加 surface_mention_premise_ids。标注导入同时接受 R_surf 与 R_task。测试构造“文本提到 p1、值不依赖 p1”。SURF-01 供后续文本保留/符号替换，本阶段只保存字段，不实现嫁接。若必须改 schema.py，只加字段，不改枚举语义。
  </action>
  <verify>
    <automated>python -m pytest tests/test_graphs_annotations.py -q --tb=short -k "surf or surface"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_graphs_annotations.py contains a case where surface mention and task ancestor sets differ
    - schema or Event carries surface_mention_premise_ids or equivalent name surface_mentions
  </acceptance_criteria>
  <done>R_surf 与值依赖分字段，可供后续替换操作读取。</done>
</task>

<task type="auto" tdd="true">
  <name>Six-role co-grouping and leakage tests</name>
  <files>src/reasoning_diff/splits.py, tests/test_splits.py</files>
  <read_first>
    - src/reasoning_diff/splits.py
    - git show 8a1c6fa:src/reasoning_diff/splits.py
    - 01-CONTEXT.md D-03
    - .planning/research/PITFALLS.md pitfall 8
  </read_first>
  <behavior>
    - SPLIT_ROLES are probe_train, dev, direction_fit, calibration, transfer_pairs, test
    - Variants sharing base_group_id (rename, noop, two rng_stream ids, two model_refs, edit sequence k=1..2, donor pair) all receive the same role
    - GSM-Plus or fit_eligible=false groups always test
    - assert_disjoint(fit_roles, test) raises if any base_group_id overlaps
    - assign_split is a function of base_group_id plus a split_seed, not variant_id
  </behavior>
  <action>
    扩展 01-01 的 splits.py：assign_group_role(members, seed, fractions, test_only_ids)。members 含 base_group_id、variant_id、rng_ref、model_ref、edit_seq_id、donor_pair_id、fit_eligible。先分组再切角色。D-03：同源题（跨 GSM-Symbolic/GSM-Plus 的同一 family_id）共组。保存 upstream_split。

    tests/test_splits.py 按 PITFALLS 检查：改名/no-op/双 seed/双模型/连续编辑只能一个 split；改变测试标签不得改变已分配的拟合组（分配不读标签）。不要使用撤回草稿的 train/direction/transfer 名称。
  </action>
  <verify>
    <automated>python -m pytest tests/test_splits.py -q --tb=short</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_splits.py exits 0
    - src/reasoning_diff/splits.py contains direction_fit and transfer_pairs
    - A leakage test raises when the same base_group_id is in probe_train and test
  </acceptance_criteria>
  <done>六角色共组；test-only 与泄漏可被测试抓住。</done>
</task>

</tasks>

## Artifacts this phase produces

- IndependentGraph, import_annotation_jsonl, surface_mention_premise_ids
- assign_group_role, assert_disjoint, test_only lock
- CONT-01 data aspect: edit sequences share base_group_id（不实现 Prefill）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotation JSONL → R_task | 标注错误会污染全部后续标签 |
| split assignment → later fit | 泄漏使测试失效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | annotation import | high | mitigate | 拒绝 model_cot 来源；unknown 不写 0（D-02） |
| T-01-11 | Tampering | splits | high | mitigate | 先 base_group 再切角色；test-only 强制 test（D-03） |
</threat_model>

<verification>
python -m pytest tests/test_graphs_annotations.py tests/test_splits.py -q --tb=short
</verification>

<success_criteria>
独立图覆盖与 SURF 分离可测；共组与 GSM-Plus 锁可测。
</success_criteria>

<output>
Create `.planning/phases/01-data-truth-measurement/01-05-SUMMARY.md` when done
</output>
