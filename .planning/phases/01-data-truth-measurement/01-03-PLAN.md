---
phase: 01-data-truth-measurement
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/reasoning_diff/tasks/t2_gsm_symbolic.py
  - src/reasoning_diff/tasks/t2_gsm_plus.py
  - tests/test_t2_gsm.py
  - tests/fixtures/t2_symbolic_one.json
  - tests/fixtures/t2_gsmplus_one.json
  - tests/fixtures/t2_formula_sidecar.json
autonomous: true
requirements:
  - DATA-02
  - DATA-03
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-02: GSM-Symbolic 与 GSM-Plus 均有读取、合法编辑、更新 answer_spec、导入公式侧车、域内评分入口"
    - "D-02: 无逐步图时 graph_status 为 unknown 或 partial，不得用 solution 文本填满 DAG"
    - "D-03: GSM-Plus 记录 fit_eligible=false 且默认角色 test，不能被 assign_split 划进拟合角色"
    - "DATA-02: reversing operation 与 critical-thinking/insufficient_information 分状态保存"
  artifacts:
    - src/reasoning_diff/tasks/t2_gsm_symbolic.py
    - src/reasoning_diff/tasks/t2_gsm_plus.py
    - tests/test_t2_gsm.py
  key_links:
    - "family_id from original_id / normalized seed_question"
    - "formula sidecar → new_oracle after legal numeric edit"
  prohibitions:
    - statement: "Do not eval untrusted template strings"
      status: required
      verification: pytest
    - statement: "Do not use GSM-Plus for probe_train/dev/direction_fit/calibration/transfer_pairs"
      status: required
      verification: pytest
---

<objective>
实现 T2 两个真实读取适配器（GSM-Symbolic、GSM-Plus）：家族归组、公式侧车、合法编辑与 test-only 锁。

Purpose: DATA-02/D-02/D-03 的数学自然语言域不能只停留在空 schema。
Output: t2 加载器、侧车、编辑与评分函数，全部用仓库内一条记录夹具验证。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/FEATURES.md
@.planning/phases/01-data-truth-measurement/01-CONTEXT.md
@.planning/phases/01-data-truth-measurement/01-RESEARCH.md
@src/reasoning_diff/schema.py
@src/reasoning_diff/splits.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>GSM-Symbolic reader, family key, formula sidecar</name>
  <files>src/reasoning_diff/tasks/t2_gsm_symbolic.py, tests/test_t2_gsm.py, tests/fixtures/t2_symbolic_one.json, tests/fixtures/t2_formula_sidecar.json</files>
  <read_first>
    - .planning/research/FEATURES.md (T2 GSM-Symbolic)
    - src/reasoning_diff/schema.py
    - 01-CONTEXT.md D-02
  </read_first>
  <behavior>
    - load_gsm_symbolic_record(obj, source_kind="fixture") maps id, instance, question, answer, original_id into Task
    - family_id uses original_id (or id_orig), never instance alone
    - Without sidecar, graph_status is unknown and task labels are not all false
    - With sidecar, graph_status may be complete or partial; nodes come from sidecar variables/ops, not from answer text
    - Domain score extract_gsm_hash_answer(".... #### 12") equals canonical 12
  </behavior>
  <action>
    手写一条 symbolic 形状记录（id, instance, question, answer 含 #### 行, original_id, original_question, original_answer, canary）。实现 load_gsm_symbolic_record。family_id = original_id。instance 只进 metadata。

    load_formula_sidecar(path) 读取变量绑定、输入句 span、运算、约束、中间事件。受限解释器只允许 + - * / 与声明变量，禁止 eval 模板字符串。无侧车时 graph_status=unknown。D-02：不得用答案冒充完整 DAG。source_kind 由调用方传入。canary 不得写入模型可见 question。
  </action>
  <verify>
    <automated>python -m pytest tests/test_t2_gsm.py -q --tb=short -k "symbolic or sidecar or family"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_t2_gsm.py -k "symbolic or sidecar or family" exits 0
    - src/reasoning_diff/tasks/t2_gsm_symbolic.py contains load_gsm_symbolic_record and load_formula_sidecar
    - family_id is taken from original_id in the test fixture
  </acceptance_criteria>
  <done>Symbolic 记录可加载；家族键稳定；无侧车则为 unknown 图。</done>
</task>

<task type="auto" tdd="true">
  <name>GSM-Plus reader and test-only lock</name>
  <files>src/reasoning_diff/tasks/t2_gsm_plus.py, tests/test_t2_gsm.py, tests/fixtures/t2_gsmplus_one.json</files>
  <read_first>
    - .planning/research/FEATURES.md (T2 GSM-Plus)
    - src/reasoning_diff/splits.py
    - 01-CONTEXT.md D-03
  </read_first>
  <behavior>
    - load_gsm_plus_record sets fit_eligible False, upstream_split test, source dataset gsm_plus
    - assign_split / require_split refuses probe_train, dev, direction_fit, calibration, transfer_pairs for that base_group_id
    - perturbation_type reversing operation is stored as query_target_change, not as operator_swap
    - perturbation_type critical thinking maps to insufficient_information, not inconsistent_constraints
    - Unresolved seed_question match stores seed_text_hash and match_status unmatched
  </behavior>
  <action>
    实现 load_gsm_plus_record(obj, source_kind)。字段 question, solution, answer, perturbation_type, seed_question, seed_solution, seed_answer。solution 不得自动变成 nodes。family_id 优先规范化 seed_question 的可复查匹配；失败则 seed_text_hash + unmatched。

    在 t2_gsm_plus.py 导出 TEST_ONLY=True 与 assert_gsm_plus_not_for_fit(role)。splits.assign_split 若记录带 fit_eligible False，必须返回 test（D-03）。不要下载 HuggingFace 数据。
  </action>
  <verify>
    <automated>python -m pytest tests/test_t2_gsm.py -q --tb=short -k "plus or test_only or reversing or critical"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_t2_gsm.py asserts GSM-Plus cannot occupy probe_train
    - load_gsm_plus_record sets fit_eligible to False
    - reversing and critical-thinking types are distinct enum/status strings
  </acceptance_criteria>
  <done>GSM-Plus 固定评测；缺失图保持 unknown。</done>
</task>

<task type="auto" tdd="true">
  <name>Legal numeric edits and domain scoring</name>
  <files>src/reasoning_diff/tasks/t2_gsm_symbolic.py, src/reasoning_diff/tasks/t2_gsm_plus.py, tests/test_t2_gsm.py</files>
  <read_first>
    - src/reasoning_diff/edits.py
    - src/reasoning_diff/tasks/t2_gsm_symbolic.py
    - FEATURES.md T2 合法编辑建议
  </read_first>
  <behavior>
    - apply_gsm_numeric_edit with sidecar recomputes answer_spec; keeping the old #### answer is rejected
    - Domain checks can mark validity invalid for negative counts when sidecar says non-negative
    - score_gsm_answer(pred, gold) uses canonical_value and returns {metric, value, denominator=1}
    - No-op inject edit is not labeled no-op unless sidecar says the sentence is a non-ancestor
  </behavior>
  <action>
    实现 apply_gsm_numeric_edit 与 score_gsm_answer。编辑必须更新 answer_spec。no-op 需要侧车无关性证据，不能仅因插入一句且最终答案碰巧不变就标 no-op。D-02：合法失败也可追溯（validity=invalid + reason）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_t2_gsm.py -q --tb=short</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_t2_gsm.py exits 0
    - A test fails the adapter if it reuses the pre-edit answer after a numeric edit that sidecar says changes the gold
  </acceptance_criteria>
  <done>T2 合法编辑更新真值；域内评分可离线运行。</done>
</task>

</tasks>

## Artifacts this phase produces

- load_gsm_symbolic_record, load_formula_sidecar, load_gsm_plus_record, apply_gsm_numeric_edit, score_gsm_answer, assert_gsm_plus_not_for_fit
- Task.metadata keys: fit_eligible, perturbation_type, seed_text_hash, match_status

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON record → Task | 外部数据字段进入家族与拟合资格 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Elevation of privilege | GSM-Plus split | high | mitigate | fit_eligible=false；拟合角色拒绝（D-03） |
| T-01-07 | Tampering | formula sidecar | medium | mitigate | 受限表达式解释器，不 eval 模板 |
</threat_model>

<verification>
python -m pytest tests/test_t2_gsm.py -q --tb=short
</verification>

<success_criteria>
两条 T2 夹具记录可读取、编辑、评分；GSM-Plus 不能进入拟合角色。
</success_criteria>

<output>
Create `.planning/phases/01-data-truth-measurement/01-03-SUMMARY.md` when done
</output>
