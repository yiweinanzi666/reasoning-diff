---
phase: 01-data-truth-measurement
plan: 06
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - src/reasoning_diff/events.py
  - tests/test_events.py
  - tests/fixtures/traces/dup_scope.json
autonomous: true
requirements:
  - MEAS-01
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-04: 两个作用域的同值 x=3 不对齐；交换两事件数值不改变匹配"
    - "D-04: q=p1+p2 中 p1 改值仍对齐到同一任务事件；不同节点同值不能对齐"
    - "MEAS-01: alignment 显式保存 matched/missing/merged/split/version_changed/ambiguous"
    - "删除、合并、解析失败只增加对应状态计数，不增加行为正例或负例"
  artifacts:
    - src/reasoning_diff/events.py
    - tests/test_events.py
    - tests/fixtures/traces/dup_scope.json
  key_links:
    - "EventIdentity.key() → align_events → Alignment + audit JSONL"
  prohibitions:
    - statement: "Do not use output numeric equality as event identity"
      status: required
      verification: pytest
---

<objective>
把事件身份与对齐做成可导出人工核查的合同：身份不含值；结构变化与解析失败可计数。

Purpose: MEAS-01 / D-04，防止后续行为标签被错配污染。
Output: 扩展 events.py、对齐状态、审计导出。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@src/reasoning_diff/events.py
@src/reasoning_diff/schema.py
@docs/EXPERIMENT_PROTOCOL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Identity without values; version and scope</name>
  <files>src/reasoning_diff/events.py, tests/test_events.py, tests/fixtures/traces/dup_scope.json</files>
  <read_first>
    - src/reasoning_diff/events.py
    - git show 8a1c6fa:src/reasoning_diff/events.py
    - .planning/research/PITFALLS.md pitfall 1
    - 01-CONTEXT.md D-04
  </read_first>
  <behavior>
    - EventIdentity.key() is a canonical JSON of entity_or_expression, occurrence_version, scope only
    - Two scopes both writing x=3 do not pair
    - Swapping the two numeric values leaves the same pairing
    - Second assignment to the same symbol in one scope increments occurrence_version
    - parse_failed records are emitted when the line is ambiguous (two assignments), not dropped silently
  </behavior>
  <action>
    扩展 01-01 的 events.py。身份规范表达式引用稳定 premise/node id，不把被编辑数值当唯一身份。D-04：对齐靠身份/表达式/版本/作用域。夹具 dup_scope.json 含两作用域 x=3 与同作用域两版本。从 8a1c6fa 可复用 parse 思路，但必须把 parse 失败显式写成 outcome/alignment=parse_failed 或 ambiguous，而不是跳过。
  </action>
  <verify>
    <automated>python -m pytest tests/test_events.py -q --tb=short -k "identity or scope or version"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_events.py includes the two-scope x=3 case and the value-swap case from PITFALLS
    - EventIdentity.key input fields do not include value
  </acceptance_criteria>
  <done>身份稳定；值交换不改匹配。</done>
</task>

<task type="auto" tdd="true">
  <name>Alignment states including merge/split/disappear</name>
  <files>src/reasoning_diff/events.py, tests/test_events.py</files>
  <read_first>
    - src/reasoning_diff/events.py
    - ARCHITECTURE.md Alignment 补充产物
  </read_first>
  <behavior>
    - align_events returns pairs plus lists missing, merged, split, version_changed, ambiguous
    - A disappeared event increments missing, not behavior changed
    - Two base events collapsing to one comparison event increment merged
    - Duplicate identities in one trace raise or mark ambiguous; they are not silently last-write-wins
  </behavior>
  <action>
    将 8a1c6fa 的 removed/added 扩展为 ARCHITECTURE 的 Alignment 状态：matched, missing, merged, split, version_changed, ambiguous。合并或分岔不得记成普通值变化。D-04：结构变化与值变分开。
  </action>
  <verify>
    <automated>python -m pytest tests/test_events.py -q --tb=short -k "align or merge or missing or ambiguous"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - align_events output includes keys missing, merged, split, version_changed, ambiguous
    - A merge fixture does not increment behavior-positive counts
  </acceptance_criteria>
  <done>结构对齐状态可机读。</done>
</task>

<task type="auto" tdd="true">
  <name>Human audit export and backfill</name>
  <files>src/reasoning_diff/events.py, tests/test_events.py</files>
  <read_first>
    - src/reasoning_diff/events.py
    - REQUIREMENTS MEAS-01
  </read_first>
  <behavior>
    - export_audit_jsonl writes one row per event and per unresolved alignment with record_id, identity, spans, parser_version, status
    - import_audit_jsonl backfills human decisions without rewriting the original events.jsonl in place
    - Rows for parse_failed, ambiguous, missing, merged are present even when values match
  </behavior>
  <action>
    实现 export_audit_jsonl(path, events, alignment) 与 import_audit_jsonl(path) -> 新版本事件列表。修正标注附 old_version_ref 与 reason。D-04/MEAS-01：重复、作用域、未对齐、消失/合并、解析失败均可导出。不在原 JSONL 静默覆盖（ARCHITECTURE 不变量）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_events.py -q --tb=short</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_events.py exits 0
    - export_audit_jsonl and import_audit_jsonl exist
    - Backfill writes a new file or new record_id, not an in-place silent overwrite of the source jsonl path
  </acceptance_criteria>
  <done>人工核查可导出与回填，原事件文件不静默改写。</done>
</task>

</tasks>

## Artifacts this phase produces

- EventIdentity, align_events, export_audit_jsonl, import_audit_jsonl
- Alignment states: matched, missing, merged, split, version_changed, ambiguous

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed text → Event identity | 错配会污染全部行为标签 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | align_events | high | mitigate | 身份不含值；歧义显式 ambiguous（D-04） |
| T-01-13 | Repudiation | audit export | medium | mitigate | 回填保留 old_version_ref，禁止原地静默覆盖 |
</threat_model>

<verification>
python -m pytest tests/test_events.py -q --tb=short
</verification>

<success_criteria>
PITFALLS 身份夹具与结构状态、审计导出均通过。
</success_criteria>

<output>
Create `.planning/phases/01-data-truth-measurement/01-06-SUMMARY.md` when done
</output>
