---
phase: 01-data-truth-measurement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/reasoning_diff/__init__.py
  - src/reasoning_diff/schema.py
  - src/reasoning_diff/artifacts.py
  - src/reasoning_diff/io.py
  - src/reasoning_diff/cli.py
  - src/reasoning_diff/tasks/__init__.py
  - src/reasoning_diff/tasks/t1_fixture.py
  - src/reasoning_diff/graphs.py
  - src/reasoning_diff/edits.py
  - src/reasoning_diff/splits.py
  - src/reasoning_diff/events.py
  - src/reasoning_diff/measure.py
  - tests/conftest.py
  - tests/test_tracer_t1_prepare.py
  - tests/fixtures/t1_tiny.json
  - .planning/PAPER_TRACEABILITY.md
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - MEAS-01
  - MEAS-02
user_setup: []

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-01: 自建 T1 夹具记录 source_kind=fixture，且不得被写成 official"
    - "D-05: python -m pytest 可在无 GPU、无下载条件下跑通 T1 夹具 prepare 全路径"
    - "D-03: 夹具基础题被赋为六角色之一（probe_train/dev/direction_fit/calibration/transfer_pairs/test），同 base_group_id 不变体分到不同角色"
    - "D-04: tracer 路径产出 Observation.outcome 与分开的 S/M raw/noise/excess；无 sham 协议时 excess 为 JSON null"
  artifacts:
    - pyproject.toml
    - src/reasoning_diff/cli.py
    - src/reasoning_diff/schema.py
    - src/reasoning_diff/artifacts.py
    - tests/test_tracer_t1_prepare.py
    - tests/fixtures/t1_tiny.json
  key_links:
    - "reasoning-diff prepare → Task/Edit/Split/Event/Observation → runs/ JSONL + manifest.json"
    - "schema enumerations consumed by later T2–T4 and measure plans"
  prohibitions:
    - statement: "Do not download datasets, tokenizer files, or model weights"
      status: required
      verification: pytest
    - statement: "Do not train probes or collect GPU traces"
      status: required
      verification: pytest
    - statement: "Do not import facebookresearch iGSM tools.tools"
      status: required
      verification: pytest
---

<objective>
打通 Phase 1 唯一生产级薄切片：包、产物合同、T1 自建夹具 → prepare CLI → 任务图/编辑/划分/事件/观测/S/M/TO。后续计划只扩展域适配与边界，不再改架构。

Purpose: 先证明身份与测量合同可执行，避免空建全部适配器。
Output: `reasoning_diff` 包、`reasoning-diff prepare`、T1 fixture tracer 测试。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-truth-measurement/01-CONTEXT.md
@.planning/phases/01-data-truth-measurement/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@docs/EXPERIMENT_PROTOCOL.md
@AGENTS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end T1 fixture prepare — one path only</name>
  <files>src/reasoning_diff/schema.py, src/reasoning_diff/tasks/t1_fixture.py, src/reasoning_diff/graphs.py, src/reasoning_diff/edits.py, src/reasoning_diff/splits.py, src/reasoning_diff/events.py, src/reasoning_diff/measure.py, src/reasoning_diff/cli.py, tests/test_tracer_t1_prepare.py, tests/fixtures/t1_tiny.json</files>
  <read_first>
    - .planning/phases/01-data-truth-measurement/01-CONTEXT.md (D-01..D-05)
    - .planning/research/ARCHITECTURE.md (Task/Event/Observation/Label/Report 最小字段)
    - docs/EXPERIMENT_PROTOCOL.md §2–3
    - git show 8a1c6fa:src/reasoning_diff/schema.py
    - git show 8a1c6fa:src/reasoning_diff/measurement.py
  </read_first>
  <behavior>
    - Loading tests/fixtures/t1_tiny.json via prepare yields Task.source_kind == fixture and graph_status == complete
    - One numeric edit on a declared premise updates answer_spec using the fixture DAG, not a cached lookup table
    - assign_split(base_group_id) returns one of probe_train, dev, direction_fit, calibration, transfer_pairs, test
    - Two fixture traces with different values for the same EventIdentity still align as one pair
    - compute_densities on the PITFALLS T/B/P example returns rho_S_raw=0.5 and rho_M_raw=0.5
    - compute_to_csp on fixture token_ids returns finite to_all or JSON-null when both empty
    - Missing sham protocol yields excess JSON-null, never 0
  </behavior>
  <action>
    先写 tests/test_tracer_t1_prepare.py（必须失败），再实现一条 prepare 路径。tests/fixtures/t1_tiny.json 为自建两前提一节点算术题（含零乘或抵消之一即可），字段 source_kind 固定为 fixture。

    schema.py 定义常量 SCHEMA_VERSION=1 与枚举：SOURCE_KINDS=(official, project_derived, fixture)；GRAPH_STATUSES=(complete, partial, unknown)；SPLIT_ROLES=(probe_train, dev, direction_fit, calibration, transfer_pairs, test) — 按 D-03，不要使用撤回草稿里的 train/direction/transfer；OUTCOMES=(changed, no_change, structural, unaligned, parse_failed, generation_failed)；SCAN_STATES 另含 unscanned。所有记录含 schema_version, record_id, run_id, base_group_id, status。

    Task 字段按 ARCHITECTURE：task_id, base_group_id, variant_id, tier, source, source_kind, upstream_id, upstream_split, premises[], question, answer_spec, graph_ref, edit_ref, graph_status。Premise 含 premise_id、原文、字符区间。Node 允许父节点为更早的 node_id 或 premise_id（修正 8a1c6fa 只允许前提的错误）。

    EventIdentity 只含 entity_or_expression, occurrence_version, scope；值不得进入 key()。Observation 必须带 outcome 与 alignment_ref。

    t1_fixture.py 导出 load_t1_fixture(path) -> Task。graphs.py 导出 ancestors(task) 与 graph_status。edits.py 导出 apply_value_edit(task, premise_id, new_literal) -> Edit，重算走拓扑表达式，不读旧 lookup。splits.py 导出 assign_split(base_group_id, seed, fractions) 与 assert_same_role(members)。events.py 导出 parse_fixture_events 与 align_events。measure.py 导出 compare_pair, build_labels, dependency_densities, lcs_overlap, preservation_to_csp。

    cli.py 提供 main(argv)：子命令 prepare --fixture tests/fixtures/t1_tiny.json --out-dir 传入临时目录；写出 tasks.jsonl、edits.jsonl、splits.jsonl、events.jsonl、observations.jsonl、labels.jsonl、run_spec.json、manifest.json。D-05：无 Web/DB。D-01：夹具路径不得把 source_kind 写成 official。
  </action>
  <verify>
    <automated>python -m pytest tests/test_tracer_t1_prepare.py -q --tb=short</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_tracer_t1_prepare.py exists and python -m pytest tests/test_tracer_t1_prepare.py exits 0
    - src/reasoning_diff/schema.py defines SPLIT_ROLES containing probe_train and transfer_pairs and does not define a split token named train as a role
    - src/reasoning_diff/schema.py defines SOURCE_KINDS containing fixture and official
    - tests/fixtures/t1_tiny.json contains the exact key source_kind with value fixture
    - src/reasoning_diff/cli.py contains def main
    - EventIdentity.key documentation or implementation uses entity_or_expression and does not hash the event numeric value into the identity
  </acceptance_criteria>
  <done>一条 T1 fixture prepare 路径从夹具读到 S/M 与 TO/CSP 可在本机 pytest 下复现。</done>
</task>

<task type="auto" tdd="true">
  <name>Artifacts, run_spec, manifest, atomic IO</name>
  <files>src/reasoning_diff/artifacts.py, src/reasoning_diff/io.py, tests/test_artifacts.py</files>
  <read_first>
    - src/reasoning_diff/schema.py
    - .planning/research/ARCHITECTURE.md (Immutable Manifests and Resume)
    - git show 8a1c6fa:src/reasoning_diff/io.py
    - .planning/phases/01-data-truth-measurement/01-CONTEXT.md (D-05)
  </read_first>
  <behavior>
    - write_jsonl then read_jsonl round-trips a Task dict
    - numpy.savez + numpy.load(..., allow_pickle=False) round-trips a 1-d int array
    - write_run_spec includes input file sha256, SCHEMA_VERSION, code revision key, and rng section
    - write_manifest lists output file hashes, record counts, and upstream manifest ids; its own digest field does not include itself
    - encode rejects float nan (allow_nan=False)
    - atomic_text replaces the target path only after flush
  </behavior>
  <action>
    实现 io.py：encode（json.dumps ensure_ascii=False sort_keys=True allow_nan=False）、digest、file_digest、atomic_text、write_json、read_json、write_jsonl、read_jsonl、write_npz(path, arrays) 内部 numpy.savez、read_npz 使用 numpy.load(path, allow_pickle=False)。D-05：JSONL 存变长记录，NPZ 只存数组。

    artifacts.py 实现 write_run_spec(out_dir, spec: dict) 与 write_manifest(out_dir, files, counts, upstream_ids)。run_spec 至少含 schema_version, input_hashes, config, protocol_version, code_revision, environment, rng。identity 字段禁止使用字面量 latest 作为版本。manifest 含 file_hashes, record_counts, array_shapes, success_count, failure_count, upstream_manifest_ids。D-01：run_spec 必须记录每个输入的 source_kind。

    tests/test_artifacts.py 覆盖上述行为；不创建 runs/ 提交物，测试用 tmp_path。
  </action>
  <verify>
    <automated>python -m pytest tests/test_artifacts.py -q --tb=short</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_artifacts.py exits 0
    - src/reasoning_diff/io.py contains allow_pickle=False
    - src/reasoning_diff/io.py contains allow_nan=False
    - src/reasoning_diff/artifacts.py contains write_run_spec and write_manifest
  </acceptance_criteria>
  <done>run_spec 与 manifest 可原子写出并按哈希复读。</done>
</task>

<task type="auto">
  <name>Package, CLI entry, pytest config, paper ledger stub</name>
  <files>pyproject.toml, src/reasoning_diff/__init__.py, src/reasoning_diff/tasks/__init__.py, tests/conftest.py, .planning/PAPER_TRACEABILITY.md</files>
  <read_first>
    - pyproject.toml (may be absent)
    - .planning/research/STACK.md (Phase 1: numpy + pytest only)
    - .planning/phases/01-data-truth-measurement/01-RESEARCH.md
    - git show 8a1c6fa:pyproject.toml
  </read_first>
  <action>
    创建 pyproject.toml：name reasoning-diff，requires-python &gt;=3.11，dependencies 仅 numpy&gt;=1.26,&lt;3，optional-dependencies.dev 仅 pytest&gt;=8,&lt;10。scripts：reasoning-diff = reasoning_diff.cli:main。setuptools packages.find where=["src"]。[tool.pytest.ini_options] testpaths=["tests"] pythonpath=["src"]。不要加入 torch、transformers、datasets、scipy、scikit-learn。

    src/reasoning_diff/__init__.py 导出 SCHEMA_VERSION。tasks/__init__.py 只做包标记，不建插件注册表。tests/conftest.py 把仓库根与 src 放入 sys.path（若 pytest pythonpath 已够则可仅放 fixture 根路径）。

    按 CONTEXT specifics 写 .planning/PAPER_TRACEABILITY.md：表头含 source file/hash、section、requirement id、impl path、verify command、local status、server status。至少登记 DATA-01、DATA-02、DATA-03、MEAS-01、MEAS-02、SURF-01、CONE-01 行，实现列可先填本阶段将创建的模块路径。D-05：不提交 runs/ 数据。
  </action>
  <verify>
    <automated>python -c "import pathlib; p=pathlib.Path('pyproject.toml').read_text(encoding='utf-8'); assert 'reasoning-diff' in p and 'torch' not in p and 'transformers' not in p; import reasoning_diff; from reasoning_diff.schema import SCHEMA_VERSION, SPLIT_ROLES; assert SCHEMA_VERSION==1; assert 'probe_train' in SPLIT_ROLES"</automated>
    <fails_when>non-zero exit, or AssertionError, or ModuleNotFoundError</fails_when>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains [project.scripts] mapping reasoning-diff to reasoning_diff.cli:main
    - pyproject.toml does not contain the strings torch or transformers
    - .planning/PAPER_TRACEABILITY.md contains DATA-01 and SURF-01
    - python -c import reasoning_diff succeeds when PYTHONPATH includes src or via pytest pythonpath
  </acceptance_criteria>
  <done>包与 CLI 入口可导入；Phase 1 依赖面不含模型库。</done>
</task>

</tasks>

## Artifacts this phase produces

- SCHEMA_VERSION, SOURCE_KINDS, GRAPH_STATUSES, SPLIT_ROLES, OUTCOMES
- dataclasses/types: Premise, Node, Task, Edit, EventIdentity, Event, Observation, Label, Alignment, RunSpec, Manifest
- functions: load_t1_fixture, ancestors, apply_value_edit, assign_split, parse_fixture_events, align_events, compare_pair, build_labels, dependency_densities, lcs_overlap, preservation_to_csp, write_run_spec, write_manifest, write_jsonl, read_npz
- CLI: reasoning-diff prepare
- paths: tests/fixtures/t1_tiny.json, .planning/PAPER_TRACEABILITY.md

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture file → Task loader | 测试夹具进入科学记录 |
| prepare → runs/ JSONL | 产物被后续阶段当输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | t1_fixture loader | high | mitigate | source_kind 由加载器写入 fixture；拒绝把夹具标成 official（D-01） |
| T-01-02 | Tampering | NPZ read | high | mitigate | numpy.load allow_pickle=False |
| T-01-03 | Information disclosure | run_spec | low | accept | 本机科研包，不写密钥；runs/ gitignored |
| T-01-SC | Tampering | pyproject deps | high | mitigate | Phase 1 只声明已核查的 numpy/pytest，不新增未审计包 |
</threat_model>

<verification>
python -m pytest tests/test_tracer_t1_prepare.py tests/test_artifacts.py -q --tb=short
</verification>

<success_criteria>
T1 fixture prepare 全路径 pytest 通过；source_kind/split 枚举符合 D-01/D-03；产物有哈希清单。
</success_criteria>

<output>
Create `.planning/phases/01-data-truth-measurement/01-01-SUMMARY.md` when done
</output>
