---
phase: 01-data-truth-measurement
plan: 07
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-06
files_modified:
  - src/reasoning_diff/measure.py
  - tests/test_measure.py
  - tests/fixtures/measure/sm_example.json
  - tests/fixtures/measure/xy_zero.json
autonomous: true
requirements:
  - MEAS-02
  - CONE-01
user_setup: []

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-04: S 与 M 分开；raw、noise_reference、signed excess 并列；负 excess 不截断为 0"
    - "D-04: 零分母或未声明 sham 协议时 excess 与 corrected 为 JSON null"
    - "MEAS-02: sham 机会与编辑扫描使用相同 (event, premise, edit_kind) 集合与相同 ANY 次数"
    - "TO_all 与 TO_clean 分开；CSP 只在已对齐干净事件上计算并带覆盖率"
    - "CONE-01: cone(delta_P)、oracle_mask、behavior_mask 分字段；xy=0 单点扫描反例阻止联合编辑 soundness 宣称"
  artifacts:
    - src/reasoning_diff/measure.py
    - tests/test_measure.py
    - tests/fixtures/measure/sm_example.json
    - tests/fixtures/measure/xy_zero.json
  key_links:
    - "Observation → build_labels → dependency_densities"
    - "preservation_to_csp + dirty_cone masks"
  prohibitions:
    - statement: "Do not clip negative excess to zero"
      status: required
      verification: pytest
    - statement: "Do not treat a single-point no-change as joint-edit soundness"
      status: required
      verification: pytest
    - statement: "Do not invent a premise identity for unpaired seed-diff noise"
      status: required
      verification: pytest
---

<objective>
实现观测谱系、S/M/噪声参照、TO/CSP 与脏锥/Oracle/行为掩码合同（含单点扫描反例）。

Purpose: Phase 1 成功标准 4–5 与 MEAS-02 / CONE-01；不训练探针。
Output: measure.py 扩展与独立夹具测试。
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/research/PITFALLS.md
@docs/EXPERIMENT_PROTOCOL.md
@src/reasoning_diff/measure.py
@src/reasoning_diff/events.py
@src/reasoning_diff/schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Observations, scan states, sham opportunity protocol</name>
  <files>src/reasoning_diff/measure.py, tests/test_measure.py</files>
  <read_first>
    - src/reasoning_diff/measure.py
    - src/reasoning_diff/events.py
    - docs/EXPERIMENT_PROTOCOL.md §2 items 4–7
    - 01-CONTEXT.md D-04 and Discretion sham protocol
  </read_first>
  <behavior>
    - compare_pair on same rng_stream + edit records outcome changed or no_change for aligned value diffs
    - unscanned, no_change, and generation_failed remain three distinct stored states after serialization
    - Sham rows use the same (event_id, premise_id, edit_kind) keys as the behavior scan; ANY aggregation uses the same attempt count
    - A lone different-rng value diff without that protocol does not create a per-premise noise label
    - Increasing edit attempts from 1 to 10 also increases sham opportunities to 10
  </behavior>
  <action>
    扩展 Observation：reference_trace, comparison_trace, edit_id, premise_id, rng_pair, event_pair, outcome, raw_values, alignment_ref, protocol_ref。outcome 使用 schema OUTCOMES；另存 scan_state 含 unscanned。

    Sham 协议名 sham_matched_opportunities：机会集合 = 行为扫描的 (event, premise, edit_kind)，聚合 = ANY，次数必须相等。协议缺失时仍可输出 raw，但 noise_reference 与 excess 为 null（Discretion + D-04）。不要把一对异 seed 轨迹的值差广播成每个 premise 的噪声依赖。
  </action>
  <verify>
    <automated>python -m pytest tests/test_measure.py -q --tb=short -k "obs or sham or unscanned or protocol"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - Serialization of unscanned, no_change, and generation_failed remains three different strings
    - A test fails if ANY-over-10 behavior is subtracted from a single sham rate
    - Missing protocol_ref yields excess None
  </acceptance_criteria>
  <done>观测谱系完整；sham 与扫描同机会。</done>
</task>

<task type="auto" tdd="true">
  <name>S/M densities with signed excess and null denominators</name>
  <files>src/reasoning_diff/measure.py, tests/test_measure.py, tests/fixtures/measure/sm_example.json</files>
  <read_first>
    - git show 8a1c6fa:src/reasoning_diff/measurement.py
    - .planning/research/PITFALLS.md pitfall 3
    - docs/EXPERIMENT_PROTOCOL.md §2
  </read_first>
  <behavior>
    - For T={p1,p2}, B={p2,p3}, P={p1,p2,p3,p4}: S={p3}, M={p1}, both densities 1/2
    - If p1 is unscanned, M does not claim p1 as confirmed miss
    - raw=0.2 and noise=0.3 yield excess=-0.1
    - Zero eligible denominator returns null for raw/noise/excess, not 0
    - Unknown task cells are excluded from M, not treated as 0
  </behavior>
  <action>
    实现 dependency_densities 使 rho_S = |B\\T| / |P\\T|、rho_M = |T\\B| / |T| 在所需标签已知的交集上计算。S 与 M 分键。保存 raw、noise_reference、excess=raw-noise_reference、denominator、coverage。负差不截断。从 8a1c6fa 起步但必须：unknown 不进 M；无 sham 则 excess null；不要把 -1 填成行为 0。D-04。
  </action>
  <verify>
    <automated>python -m pytest tests/test_measure.py -q --tb=short -k "density or excess or null or SM or sm"</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/test_measure.py encodes the PITFALLS T/B/P example with densities 0.5 and 0.5
    - tests/test_measure.py asserts excess == -0.1 for raw 0.2 noise 0.3
    - empty denominator returns JSON-null / Python None
  </acceptance_criteria>
  <done>S/M 分列；有符号差值与 null 分母符合协议。</done>
</task>

<task type="auto" tdd="true">
  <name>TO/CSP and CONE-01 masks plus xy counterexample</name>
  <files>src/reasoning_diff/measure.py, tests/test_measure.py, tests/fixtures/measure/xy_zero.json</files>
  <read_first>
    - docs/EXPERIMENT_PROTOCOL.md §2 item 8
    - Reasoning-Diff-修订方案-v3 (1).md TO/CSP and §2.5 cone
    - .planning/REQUIREMENTS.md CONE-01
    - .planning/research/PITFALLS.md pitfall 2 xy fixture
  </read_first>
  <behavior>
    - TO = 2*LCS(T0,Tpert)/(|T0|+|Tpert|); TO_all uses full token_ids; TO_clean uses tokens of aligned clean events only
    - CSP_matched is mean of value-equal indicators on A_clean; empty A_clean → null
    - Clean events are selected by independent R_task (not by “value happened not to change”)
    - dirty_cone(delta_P, r_hat) returns steps whose predicted or oracle set intersects delta_P; oracle_mask and behavior_mask are stored separately
    - Fixture f(x,y)=xy at (0,0): editing only x or only y yields no_change; editing both to (1,1) yields changed. A function claim_joint_soundness_from_singletons must return False
  </behavior>
  <action>
    实现 preservation_to_csp、dirty_cone、oracle_mask_from_rtask、behavior_mask_from_rhat。CONE-01：三套集合分字段，禁止合成一个“去噪真值图”。TO/CSP 同时报 coverage、dirty_change_rate、structural counts、failures。

    xy_zero.json 描述 f=xy 在 (0,0) 的任务图与三次编辑。该夹具必须让任何“单点未变 ⇒ 联合编辑也安全”的辅助函数返回 False。本阶段不实现嫁接执行器。
  </action>
  <verify>
    <automated>python -m pytest tests/test_measure.py -q --tb=short</automated>
    <fails_when>non-zero exit, or "0 passed" in the pytest summary, or the word "failed" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - python -m pytest tests/test_measure.py exits 0
    - measure.py contains dirty_cone and preservation_to_csp or equivalent names to_all / csp
    - tests/test_measure.py contains the xy (0,0) singleton vs joint case
    - TO_all and TO_clean are distinct keys in the preservation result
  </acceptance_criteria>
  <done>TO/CSP 带分母与覆盖率；cone/Oracle/行为掩码分离；单点反例可测。</done>
</task>

</tasks>

## Artifacts this phase produces

- compare_pair, build_labels, dependency_densities, preservation_to_csp, lcs_overlap
- dirty_cone, oracle_mask_from_rtask, behavior_mask_from_rhat, claim_joint_soundness_from_singletons
- Observation outcomes + sham_matched_opportunities protocol

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw observations → published densities | 截断或改分母会伪造 C3 现象 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering | dependency_densities | high | mitigate | 负 excess 保留；零分母 null；S/M 分键（D-04） |
| T-01-15 | Tampering | sham protocol | high | mitigate | 无协议不得填 noise/excess；机会集合必须对齐 |
| T-01-16 | Tampering | cone masks | medium | mitigate | Oracle 与行为掩码分字段，禁止合并成单一真值图 |
</threat_model>

<verification>
python -m pytest tests/test_measure.py -q --tb=short
</verification>

<success_criteria>
PITFALLS S/M 与 xy 反例、sham 次数匹配、TO/CSP 覆盖率均被 pytest 锁住。
</success_criteria>

<output>
Create `.planning/phases/01-data-truth-measurement/01-07-SUMMARY.md` when done
</output>
