# Phase 3 Plan 03-01

**Goal:** Dual bilinear heads, boundary MLP, conformal calibration, and transfer modes.

**Must have**
1. σ(hᵀUVᵀe+b) with λ_FN=10 and unknown-label mask.
2. Boundary MLP hidden=256.
3. Conformal k=⌈(N+1)(1-α)⌉; k>N → ∞; α≥1 invalid.
4. Direct transfer refuses 4096→3584; unlabeled vs supervised maps on transfer_pairs only.
5. Both H and E inputs have an apply-map path.

**Verify:** `tests/test_science.py`, `tests/test_review_regressions.py` C-01/C-02
