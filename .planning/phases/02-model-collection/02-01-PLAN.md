# Phase 2 Plan 02-01

**Goal:** Frozen-card collection interfaces and tiny-model proofs for three positions, hooks, cache, and independent generators.

**Must have**
1. Frozen model cards with required revisions; `latest` rejected.
2. Explicit decode loop using `torch.Generator`.
3. resid_post hook last-token only, cleaned in finally.
4. pre_step / pre_value / post_step use distinct limits; straddling tokens excluded.
5. Cache isolation across intervention conditions.

**Verify:** `tests/test_tiny_hooks.py`, `tests/test_tiny_cache.py`, `tests/test_generate_loop.py`, `tests/test_review_regressions.py::test_d01_three_positions_differ_and_leak_flag_is_real`
