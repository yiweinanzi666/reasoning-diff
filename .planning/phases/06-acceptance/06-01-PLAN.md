# Phase 6 Plan 06-01

**Goal:** CLI chain, artifacts, resume/shards, docs, independent review.

**Must have**
1. Stages consume `--in-dir`.
2. Manifest includes `report.json`.
3. Atomic npz; `python -m reasoning_diff`.
4. Server runbook with real command chain.
5. Two consecutive independent A–F reviews on one frozen hash.

**Verify:** `tests/test_cli_pipeline.py`, `tests/test_review_regressions.py` E-*; audits/round-NN
